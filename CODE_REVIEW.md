# 项目代码审查与后台实验记录

## 纯窗口剩余兼容对照（2026-09-05 19:00）

新增独立 `mbv.movement_probe`，不改正式输入配置。NewMaple 只有 `NewMapleClass` 主窗口，输入句柄与根句柄均为 590382，无子窗口；探针与游戏完整性级别均为 8192。默认只读，显式启用真实输入时要求停止其它发键器、游戏后台且非最小化；每组限时 0.3 秒、最终抬键，进程身份改变/手动按键/F9/位移/总时限都会停止。

实测 8 种组合 × 左右方向共 16 组：PostMessage 与 SendMessageTimeout（40 毫秒超时）；标准参数、清扩展位参数、每次按下清先前状态位、旧式零参数。游戏在测试采样及发键检查时始终为后台，唯一小地图标记始终为像素 `(112.5, 47.5)`；所有组横纵位移均为 0，API 未报错。结果报告：`logs/movement-probe-20260905-190026-167638.json`。没有发现可用的纯窗口移动组合，未推广任何参数到日常挂机。探针已自行释放按键并退出。共 **291 项测试通过**，新增 9 项覆盖参数矩阵、同步超时、异常抬键、目标身份及输入限时。

## 移动修复与实测补充（2026-09-05 18:47）

原地策略已改为平时超出小地图安全点容差也优先回位。短步改为跨帧停攻准备、限时按键、位移验证；只在正确方向实际位移后更新计时和待回位标志。三轮无位移或反向位移暂停，普通回位连续 2 秒无进展重试、4 秒无进展暂停。输入层独立计时抬键，后台重复发键错误会交给主循环；没有切换成全局输入或伪造焦点。

新增移动回归后共 **282 项测试通过**。真实 NewMaple 测试中，游戏失焦、唯一小地图标记保持 `(0.790598, 0.415663)`，右移脉冲连续尝试 0.12/0.24/0.36 秒，测得位移均为零；日志 `session-20260905-184347.jsonl` 在第三次后记录 `movement_failed` 并暂停。故**策略与失败保护已修复，但纯窗口后台移动仍未解决**，不能将消息投递或单元测试成功等同于游戏移动成功。测试实例已通过 F9 退出；原控制面板进程仍在，但其视觉/输入会话已结束，需要用户关闭面板并重新启动。

日期：2026-09-05。对象是当前本地工作区，包含之前尚未提交的 NewMaple、玩家识别、策略和 Buff 修改。此次审查覆盖启动/档案、配置与素材、采集、视觉跟踪、三种策略、输入调度、Tk/HUD、性能监控及测试。以下是发现的问题，不代表不存在其它缺陷。

后续修复状态（2026-09-05）：用户已实测确认独立后台可挂机。本次优化完成下列第 1–6 项，第 7 项整帧配置快照及性能优化仍待处理。历史问题描述保留供追溯，代码链接的行号以最初审查时为准。

- 连续帧确认使用帧编号去重，空帧/歧义打断确认链；暂停和真实视觉记录清理旧待确认状态。
- Buff 的 0.45 秒停攻准备改为跨帧推进，发键前检查暂停/退出/窗口/前台；后台模式允许失焦。按键保持仍使用现有 0.18 秒 tap，后续可再把该阶段改成可中断状态机。
- 标飞安全区上下边界均使用小地图坐标。
- 线程异常由 Tk 主线程轮询显示，去掉会捕获已清除异常变量的延后 lambda。
- Buff 按最早到期时间调度，新增短间隔三槽位公平性回归。
- 配置采用同目录临时文件、flush/fsync、原子替换及上一版 `.bak` 备份；多实例并发覆盖仍未解决。

本次完整测试 **263 项通过**，新增 14 项可靠性回归，保留后台输入隔离与真实 Windows 消息窗口测试。未重启或改动正在运行的用户挂机会话，新代码需重启助手生效。

## 建议优先处理的缺陷

### 1. [P1] 玩家重获的“连续帧”会跨越无匹配帧累计

位置：[player_tracking.py](D:/CodexSpace/MapleBowmanVision/mbv/player_tracking.py:257)、[bot.py](D:/CodexSpace/MapleBowmanVision/mbv/bot.py:1516)。

`consider_reacquisition()` 累计 `pending_count`，但没有有效候选时不会重置；`mark_miss()` 也保留计数。实测第一次候选返回 None，调用 50 次 `mark_miss()` 后，同位置的第二个候选立即确认。实际调用者在没有候选的帧中正好走这条路径，所以“两帧确认”可以由相隔很久的两次命中满足，弱姓名板恢复和辅助认人都受影响。

建议按帧编号或连续候选状态计数；本帧无候选、候选歧义或确认类型变化时清空计数。不能直接在所有 `mark_miss()` 中无条件清零，因为合法的首个待确认帧也会调用它。增加“命中→空帧→命中仍不得确认”的回归用例。

### 2. [P1] Buff 停攻等待期间失焦，前台模式仍可能发键

位置：[bot.py](D:/CodexSpace/MapleBowmanVision/mbv/bot.py:583)。

主执行器进入 `_act()` 时检查前台，然后 `_try_auto_buff()` 等待 0.45 秒，再按键 0.18 秒；等待后没有再次检查前台或退出请求。如果这段时间用户切到其它窗口，前台模式的全局输入仍可能发到新窗口；F8/F9 请求也要等当前动作结束才处理。

建议将施法准备、按下、抬起、保护窗口改为可中断的分阶段调度，并在每次实际 keydown 前检查授权、暂停、窗口与前台条件。此次独立窗口消息模式避免向其它应用投递按键，但原前台/兼容输入流程仍需优化。

### 3. [P2] 标飞安全区混用了主画面和小地图的 Y 坐标

位置：[throwing_star.py](D:/CodexSpace/MapleBowmanVision/mbv/strategies/thief/throwing_star.py:360)。

`top` 来自小地图安全区，但 `if player_y < top` 使用主画面角色高度；同分支下方检查已经使用 `safe_player_y`。镜头移动或两种画面布局不同，就可能在角色实际位于安全区内时一直返回 `SAFE_OUTPUT_ABOVE`，或者漏掉角色高于安全区的情况。

建议统一用小地图 `safe_player_y` 比较，测试刻意让主画面与小地图 Y 不同，避免现有同数值样例掩盖错误。

### 4. [P2] 视觉线程报错后的 UI 回调可能再次报错

位置：[panel.py](D:/CodexSpace/MapleBowmanVision/mbv/panel.py:213)。

`except BaseException as exc` 内排队 `lambda: self._worker_failed(exc)`。Python 退出 except 后会清除异常变量，延后运行的 lambda 可能因找不到 `exc` 报 NameError，遮盖真正错误并阻断后续关闭流程。

建议绑定回调参数，或者把异常对象放到线程安全队列、由 Tk 主线程轮询显示。后者也能减少 worker 对 `root.after` 的跨线程依赖。

### 5. [P2] Buff 固定槽位优先级可能让后面两项永远不执行

位置：[buffs.py](D:/CodexSpace/MapleBowmanVision/mbv/buffs.py:38)。

调度总从 Buff 1 开始，施法保护为 1.2 秒。当 Buff 1 的间隔比实际调度周期短，它每次都是已到期项，Buff 2/3 无法获选。实测三项都设 1 秒，连续五次调度结果全是 `buff_1`。面板允许用户直接输入该间隔。

建议按最早到期时间或逾期程度选项，同到期轮转；也可在 UI 提示小于施法耗时的间隔无法满足。保留独立开关和原有冷却时间。

### 6. [P2] 保存配置先截断原文件，中断时可能丢失校准数据

位置：[config.py](D:/CodexSpace/MapleBowmanVision/mbv/config.py:331)。

`save_config()` 直接以 `w` 打开个人配置，再序列化。写入途中出错、程序退出或其它实例同时读写时，可能留下半个 JSON，下一次启动也无法加载。配置包含用户花时间采集的坐标、按键和素材分类选择，不能只依赖再次采集恢复。

建议先在同目录写临时文件、完成 flush/fsync 后原子替换，保留上一份可用备份；若允许多个实例同时运行，再增加写入版本校验。原子替换解决半文件，不独自解决并发覆盖。

### 7. [P2] 配置热更新不能保证一帧使用同一套配置

位置：[bot.py](D:/CodexSpace/MapleBowmanVision/mbv/bot.py:698)、[bot.py](D:/CodexSpace/MapleBowmanVision/mbv/bot.py:1783)。

`apply_config()` 替换配置、策略和 tracker 时有锁，但视觉循环没有使用该锁获取完整快照。循环先保留旧 `vision`，随后又多次读取当前 `self.config` 的区域、策略和参数；更新恰好发生在帧中间时，可能混用旧阈值与新区块，并留下旧检测结果。更新时暂停减少了即时发键风险，但不保证识别状态一致。

建议每帧取得配置/模板/策略的不可变快照和版本号；配置版本改变后丢弃旧帧结果。无需把整个 OpenCV 过程锁住，否则会卡面板。

以上为最初审查时的缺陷记录；第 1–6 项已在后续优化中处理，当前状态见文档开头。

## 性能与结构优化

- 玩家丢失时每帧全图扫描姓名板、头部、称号，随后又可能局部低阈值扫描。可以持续进行小范围恢复，低频全图搜索；先根据现有分段耗时确定预算，保留周期全图校验。
- `nameplate_identity_similarity()` 为每个候选重新生成模板字形掩码和像素统计。模板部分可像缩放特征一样缓存，候选只计算现场部分。多模板字形匹配和真实失败截图回放比继续堆阈值更值得投入。
- `bot.py` 约 2200 行、`panel.py` 约 2600 行，配置迁移、视觉追踪和动作状态互相交织。建议先提取独立 PlayerTracker、FrameSnapshot 与动作调度器，再拆面板卡片；不建议先做纯文件搬家。
- 模板 Alpha、同帧 SceneFeatures 缓存、策略注册表、模板回收及已有性能快照设计可以继续保留。没有对算法提速做基准测试，本轮不宣称具体 CPU/FPS 收益。

## 本轮已实现的后台实验

- 新增 `input.delivery=window_message`；面板选择“独立后台（实验）”即会持久化并应用。沿用当前档案校准和素材，旧模式保持可选。
- 只向一个解析后的游戏输入 HWND 投递 `WM_KEYDOWN/WM_KEYUP`，不调用 SendInput，不附加 WM_CHAR、同步重复按键消息或伪造焦点事件。Windows 消息投递成功不代表客户端实际接受技能。
- 新增 PrintWindow 客户区截图。辅助进程启动等待最多 5 秒，单次截图等待 0.8 秒；失败、空白、窗口最小化或超时会暂停，无桌面截图回退。辅助进程随退出/切换模式清理。
- 独立模式不主动切前台或置顶；失焦时隐藏游戏 HUD，避免覆盖用户当前应用。最小化和客户端停止后台绘制仍不支持；非空缓存帧尚不能可靠判定为冻结。
- 修复共用输入层的两个缺陷：前台 `release_all()` 曾未给 `held` 中的键发送 keyup；重复发键线程曾在锁外 dispatch，可能在 up 后再次 down。现在按住集合与投递顺序使用同一锁，停止重复后再释放。
- 增加只读 `python -m mbv.background_probe --profile newmaple --samples 3`，输出截图耗时和姓名板分数，不向游戏发键。

## 验证结果及边界

- 项目虚拟环境完整测试 **249 项通过**，新增 10 项验证覆盖窗口消息隔离、无全局回退、暂停释放、重复线程停止、截图超时/错误/复用、不抢焦点及不置顶。
- 使用真实 Windows 不可见消息窗口做集成测试：收到了 right 的 KEYDOWN/KEYUP，前台焦点不变，SendInput 没有被调用。这个测试窗口属于测试进程，不是 NewMaple。
- 对当前 NewMaple 实测 PrintWindow 返回 1280×720 图片，热启动后截图约 34–45 ms。一次采样姓名板 0.7069、身份分约 0.84；后一次采样原始分约 0.54、无有效身份候选。仅凭这些分数不能确定后一次低分的原因，也不能宣称后台识别稳定。
- 测试时 NewMaple 位于前台且旧助手仍在发攻击键，因此没有叠加实机按键、切焦点或遮挡测试。**NewMaple 是否在失焦后接受消息、持续渲染和正确补 Buff，仍待游戏内验证。** 本轮保留个人当前输入模式，没有自动切换运行中的会话。
- PrintWindow 由目标程序处理，且是同步调用；窗口消息与异步键盘状态也是不同机制。参考 [Microsoft PrintWindow 文档](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-printwindow)、[Microsoft 键盘输入说明](https://learn.microsoft.com/en-us/windows/win32/learnwin32/keyboard-input)。

后续优先处理整帧配置一致性，再针对实测瓶颈调整视觉扫描频率和模板字形缓存。后台运行已获用户实测确认，仍保留最小化和画面冻结等已知边界。
