# 项目代码审查与后台实验记录

## 狩猎验证只读检测与提示音（2026-09-11）

按用户收窄后的需求，只检测聊天栏固定文字并提醒在场的人，不暂停挂机，不读取数学题，不生成或提交任何聊天命令。新增 verification_alert 观察器、NewMaple 面板独立持久开关（默认关闭）及试听按钮。从用户提供截图提取固定字形二值特征，不新增 OCR 模型或下载依赖，模板不含聊天原文、角色信息或验证题目。复用截图并独立记录耗时，2 Hz 检测、3 次连续命中、提示消失 10 秒才解除锁存、60 秒提醒冷却；普通暂停不重置锁存，画面更新中断不算连续确认或连续消失。声音使用异步系统别名，不发键、切窗或设置策略状态。检测/声音失败只显示提醒器错误，磁盘日志失败不妨碍先播放提示音。

两张输入截图实测均在聊天栏匹配到关键字（位置分别为 x=8,y=614 和 x=8,y=588，相关分约 1.0），未识别验证框内容。1280×720 样本本机 50 次检测平均约 6.8 ms，仅每半秒执行一次；该数值不是所有设备性能保证。支持原生及少数常见缩放，字体/颜色/布局变化仍可能漏检；持续可见旧消息不重复提醒，也无法单靠字形区分过期消息或同屏新增的一条相同消息。面板文字明确“挂机未暂停”，人工聊天仍须自行暂停并关闭喝药。没有操作当前游戏、试听实际扬声器或重启助手；图像分析与自动化测试不能代替运行中听觉验证。

新增 18 项测试，完整 574 项通过，覆盖原生及缩放样本、限制聊天区域、部分字形/随机图/错误颜色拒绝、多帧确认、限频和锁存、截图间断、经典档案隔离、实时开关、配置迁移及落盘、异步声音、命中和异常不改 armed/策略/键盘。个人配置、截图原文件、素材均未修改，未提交或推送。

## 拾取清怪期间换边与短暂丢定位（2026-09-06）

最新 session-20260906-215249.jsonl 为 foreground：22:07:17.839 返程清怪向左近战，22:07:18.459 即改为向右 25 ms 转向，22:07:29–30 又左右换边；22:07:27.941 短暂丢视觉后立即记录向右 movement_start，约 0.1 秒后恢复攻击。代码对应三个缺口：每帧重新选左右最近目标，没有清怪侧稳定性；上轮 80 ms 保护只用于移动后第一次转向；返程清怪的 minimap_only 路径绕过了去程已有的视觉丢失宽限。日志证实这些请求序列，不能证明游戏内部朝向或哪个请求被吞掉。

路线清怪现在从当前帧、左右索敌区并集内的有效检测选择目标，优先清当前侧；该侧连续缺失 0.4 秒再换侧，不对旧框攻击、不把漏检当死亡，清怪结束清除侧状态。原侧恢复及药/Buff/定位中断重置换侧确认。仅前台路线清怪每次必要转向均使用 120 ms 稳定准备、至少 80 ms 点按（保留更长个人配置）、120 ms 抬键等待，仍保留上一技能的停攻门槛；同侧不重复转向。返程清怪时短暂视觉丢失先停走等原配置宽限，超时仍允许受限小地图回位；取消/掉层等安全返程不被拦截。其它策略、非路线攻击、非前台转向时序及混合不抢前台承诺不变。

新增 9 项回归，完整 556 项通过，覆盖三阶段侧锁、当前帧候选过滤、漏检/恢复/中断确认、清怪结束解锁、返程定位宽限、后续换边与失效后的转向保护、个人较长配置、持续输出和模式隔离。未向游戏发键、重启进程或修改个人配置/素材；仍需用户重启后实测客户端接受转向的效果。

## 前台拾取/回位后的转向交接（2026-09-06）

用户确认朝向不一致集中于拾取/回位刚结束。session-20260906-214140.jsonl 是 foreground，新拾取日志已生效：21:43:57.345 释放拾取，21:43:57.372 记录 25 ms 向右转向；后面还有同类交接。代码只依据上一次攻击计时，不等待刚释放的移动/拾取输入，短按返回就缓存方向，此后连续同侧不再重试。日志证明发送时序和请求方向，不证明客户端成功转身，不能据此认定所有朝向错误都是同一原因。

本次仅加强 foreground 普通攻击的移动/拾取后交接，真实移动发键（含周期短步和找人位移）或拾取释放标记需重转。攻击入口先释放左右与拾取，跨帧等待 120 ms 并再次确认同侧目标，再短按至少 80 ms、抬键等待 120 ms；上次攻击后的 0.6 秒停攻条件仍保留。目标变侧重新等待，失败保留重转要求，成功后恢复原同侧攻击节奏，不逐次点方向、不定期刷新。混合后台不主动切窗与其它模式原转向时序不变，个人配置和素材未修改。

新增 6 项回归并更新移动后转向契约，完整 547 项通过：拾取释放交接顺序、同侧持续输出仅一次转向、准备期目标翻边、不跳过技能等待、发送失败、用户较长短按配置，以及非前台三模式时序隔离。未向当前游戏发送测试按键或重启实例；80 ms 短按可能有少量位移，仍须用户复测实际面向，不能将单元测试当作客户端动作验证。

## 长按拾取实测失败后的重复输入修正（2026-09-06）

用户报告上一版长按无法拾取。最近 session-20260906-212829.jsonl 明确为 foreground，21:30:14–16 有连续去程位移，随后进入 PICKUP_COLLECT，没有输入异常；因此不能归因于混合后台切窗失败。日志此前没有逐次拾取按键记录，不能单凭路线状态确认游戏收到键或物品入包。

代码确认两个遗漏：hold 只首次 down，前台 _repeat_key 直接返回，混合扫描码也刻意抑制重复 keydown；没有模拟实体键盘自动重复。另一个是 down 在登记 deadline 前尝试启动前台线程，因此首次 hold 不启动、后续 down 因已持有直接返回，0.8 秒无帧释放也没有实际运行。先前测试手动调用 _repeat_key 且 mock 掉启动器，未覆盖此缺陷。Windows 官方说明长按重复产生多个 keydown，释放时才产生一次 keyup：[Keyboard Input Overview](https://learn.microsoft.com/en-us/windows/win32/inputdev/about-keyboard-input)。客户端是否仅因此漏拾取仍需实测，不宣称日志已证明其内部处理机制。

现在只对 hold 路线拾取增加约 0.1 秒自动重复按下，保持中途不抬键；先登记期限再确保线程启动。前台重复前复核焦点，混合重复沿原通道并复核窗口/取消，不抢前台或回退另一通道。普通攻击、方向键和其它 down/tap 行为不变。重复错误先抬键再上报，保持旧超时/遇怪/药/Buff/失焦及暂停释放；新增 pickup_hold_start/release 日志，明确只记录未验证的发送。

新增 7 项测试、完整 541 项通过：三种非混合模式自动重复序列与结束一次抬键、前台首次启动线程及真实线程无后续帧自动松键、失焦停止重复、普通方向键不被改变、混合双通道重复及发送失败清理。未向游戏发键、重启实例或修改个人配置，必须由用户重启后复测。

## 路线拾取持续保持按键（2026-09-06）

用户要求拾取长按。路线去程、到点停留与返程的有效拾取阶段改为 Keyboard.hold，每帧续期但不重复抬按；沿用原普通按键通道，混合模式每次按下/抬起保持原通道。旧 pickup_interval_seconds 保留为路线拾取标识，不再用于 0.15 秒点按；不改其它策略的普通零散拾取或技能输入。

持有的拾取键单独登记，遇怪、药/Buff、定位早退、移动准备/分段、结束和暂停时释放；实际攻击/转向前先释放，避免拾取键与技能重叠。输入重复线程独立检查最长 0.8 秒的续期期限，避免视觉停更后无限保持。混合到点拾取结束已有移动租约后，不再每帧 finish_movement 抬掉拾取；新移动租约前先放开自有拾取键，避免空闲检测自锁。失焦仍按原通道抬起扫描码，再由后续有效拾取帧选择消息通道，不新增切窗。

新增 8 项测试，完整 534 项通过，覆盖四种输入模式的按下/保持/释放、到点跨帧连续保持、超时松键及恢复、混合失焦切换、怪物/定位/药/Buff/分段中断、按下失败补偿和暂停清理。未向游戏发送测试按键、改个人配置或重启当前实例；客户端是否连续成功拾取仍需用户重启后实测。

## 混合后台战斗不主动切窗（2026-09-06）

用户要求仅混合模式减少攻击导致的前台切换。此前即使取消固定刷新，移动/焦点/长定位丢失使缓存失效后，攻击仍会调用可激活窗口的移动租约。现在战斗转向只借用已有前台，Keyboard/HybridMovement 增加默认保持旧行为的 allow_focus 参数；转向传 false，输入层在实际租约建立时再次检查，不因两次检查间切窗而重新激活。周期右移、拾取往返、安全回位和策略实际导航仍用原移动授权，不改变非混合模式。

由于之前已实测 PostMessage 转向无效，混合后台普通攻击/跳攻不发送方向、不伪造转向缓存，只按技能沿用游戏当前面向；只转向动作等待前台。HUD 显示后台原朝向攻击，日志 direction=null、requested_direction 单独记录并标 turn_deferred，不能声称已朝目标转身。游戏回到前台后仍按新视觉帧独立转向。后台完全丢失定位时等待恢复，不主动切窗左右找人；小地图可信导航、安全回位和攻击身份门禁不变。怪在另一侧时可能打不到，这是不抢前台与当前客户端后台转向限制之间的明确取舍。

新增 10 项测试，完整 526 项通过。覆盖真实 Keyboard/HybridMovement 加模拟桌面的后台目标换边连续输出（零焦点请求、零方向输入）、后台识别恢复/完全丢失、只转向/跳攻、前台转向、检查与租约间焦点竞争、实际回位仍可激活；原非混合模式与路线测试保持通过。未操作游戏、重启实例或改个人配置，实际效果需用户重启后观察。

## 攻击前多余位移与重复转向（2026-09-06）

用户确认优化攻击前走一步：原方向短按被强制提高至 60 ms，覆盖个人配置 25 ms；动态策略的非混合路径还在每次技能期间持有方向，混合及消息模式每 5 秒再次转向，普通药/Buff早退也清空转向记录。这些输入会产生非必要位移，不能归因于 45 秒周期短步。

所有模式的普通攻击统一独立转向，旧 face_each_attack 参数仅保留接口兼容，不再逐技能按住方向；取消所有模式的固定五秒方向刷新。短按尊重配置，下限降至 20 ms（现有默认/个人配置 25 ms），混合上限仍 100 ms；真正换边或缓存失效仍保留 0.6 秒停攻准备和 0.08 秒抬键等待。普通喝药和 Buff 准备、发键及施法保护不再单独清方向；若辅助键绑定到方向或跳跃则仍清除，焦点变化与不可信地图标记检查提前到药/Buff早退之前。真实移动、长视觉丢失、暂停/配置、新混合租约的失效机制不变，未放宽识别攻击门禁、改变周期右移或拾取路线。

新增 7 项回归、更新旧逐技能持方向/五秒刷新契约，完整 516 项通过。四种模式各 20 秒的模拟同侧攻击均仅首次转向、按 0.25 秒步进发送 80 次技能；移动后重新转向、换边等待、焦点变化跨药键门禁、地图歧义跨 Buff 门禁以及移动键绑定均覆盖。只验证了输入序列，25 ms 是否足够被客户端接受及实际位移仍需用户重启后观察，不保证零位移或视觉朝向正确；未向游戏发测试键、重启运行实例或修改个人配置。

## 鼠标位移不中断与纯前台定时转向停攻（2026-09-06）

用户明确要求不再因鼠标位移打断挂机。`session-20260906-174634.jsonl` 确有一次鼠标横向 -5 像素触发混合输入异常、随后 disarm。已删除移动租约中的光标采样和比较，同时将启动空闲检查从 GetLastInputInfo 改为按键释放后的 0.7 秒观察，否则持续鼠标位移仍会间接阻止开始移动。键盘/鼠标按钮按住、焦点变化、窗口身份、心跳和按键时限保护不变。按键观察仍是轮询，不保证捕获两次轮询之间的极短人工点击。

前台新旧会话发键间隔中位数约 0.267/0.268 秒，但共同的 `_prepare_attack_turn()` 仍每 5 秒让同向输出停攻至少 0.6 秒并再次转向。去掉纯 foreground 模式的固定刷新，只在首次、换边或移动/暂停/药/Buff/定位等状态使朝向失效时重新转向，保留真实换向的停攻及抬键等待。其它输入模式的兼容刷新不变，不改变用户攻击间隔和玩家身份门槛。

新增限频 `action_state` 日志：记录行动状态、模式、是否运行、输入视觉/目标候选、小地图、怪物候选及距上次攻击时长；变化最多每秒一次、稳定状态每 5 秒一次，覆盖上游早退，区分没有攻击日志是等转向、没有范围内目标、定位门禁还是 Buff。原日志中的全部数秒空档尚不能仅凭上述周期刷新解释，不宣称都已找到原因。只读画面确认了游戏正在输出，但助手窗口截图没有取得有效 HUD/性能读数，未据此推测具体 FPS。

新增 6 项回归并更新鼠标位移/兼容刷新旧契约；完整 509 项通过。连续同侧前台 20 秒模拟保持 80 次按配置间隔攻击、仅首次转向，超过 5 秒不再固定停攻；换边及失效仍需重新转向。未发游戏测试键、切窗、改个人配置或重启运行实例，已请用户重启后做前台对照，再用新日志核查是否还有长停顿。

## 混合普通按键按前后台选择通道（2026-09-06）

用户实测纯前台攻击正常、混合攻击偏慢。旧会话中的发键间隔中位数约为前台 0.268 秒、混合 0.279 秒，不能把 API 发键计数当作实际施法速度，也尚未证实 PostMessage 的 35 ms 短按漏接。用户明确同意混合模式在游戏前台使用 SendInput 发技能、后台继续 PostMessage，因此本轮修改输入通道，而非直接缩短转向/空闲保护或提高发键频率。

普通攻击、近身技能、Buff、喝药、拾取及跳跃攻击通过 Keyboard 的同一条焦点路由，不为每次技能激活窗口或申请移动租约。按下时固定通道与目标 HWND/PID，抬起和重复保持不跟随新焦点迁移；前台硬件键失焦后由独立约 50 ms 输入巡检释放，不向新窗口重复 keydown。每次新按下再选择通道，并记录 `hybrid_skill_channel` 变化。发送失败不换通道重试。独立后台仍绝不使用 SendInput。

自有技能扫描码另行登记，移动 watchdog 不将沿途拾取误判为手动输入；保留真实键鼠、焦点、会话时限及 80 ms 自有抬键同步宽限。暂停覆盖点按及保持键，迟到的旧点按 finally 不抬掉新会话同名键；抬键失败保留记录并锁存故障，释放前禁止重新绑定。窗口消息抬键检查原进程身份，最小化不阻断清理，HWND 被其它进程复用则不再投递。

完整 503 项测试通过，新增 17 项覆盖焦点切换、原通道抬键、前台技能/后台消息、技能与移动同时保持、输入巡检、暂停及新旧点按、失败无回退、抬键重试、根/子窗口身份和最小化清理，并更新旧“技能始终消息”的测试契约。未向实际游戏发键、切窗、改个人配置或重启实例；实际攻击速度需重启助手后选择混合模式验证。焦点检查和 SendInput 仍非 OS 原子操作，不能承诺切窗竞态完全消失。

## 混合模式空闲等待提示与抬键复核（2026-09-06）

用户进一步确认实际看到的是“等待键鼠空闲后切前台”，而游戏已经在前台；该状态来自 `prepare_movement()` 返回待命，并不是 disarm。原逻辑在前台也要求 0.7 秒键鼠空闲，`GetLastInputInfo` 的等待还可能受到本助手扫描码输入影响，但 HUD 未区分前后台。现按移动租约绑定的根窗口判断，前台显示“游戏已在前台，等待键鼠空闲后移动/转向（未暂停挂机）”，后台才显示切前台；首帧提示改为“已取得前台授权”，不再暗示一定发生过切窗。保留空闲门禁，不绕过用户操作保护，也不宣称消除了这段等待。

该会话 `session-20260906-012858.jsonl` 还另有两条真正的输入异常暂停，但旧日志无法区分键盘、鼠标位移或焦点变化。现在真正中断时分开记录原因。代码中的 keyup 后立即把本助手方向键纳入手动输入检查存在状态同步边界，增加仅针对成功抬起的自有键的 80 ms 有界宽限；其它键、鼠标和切窗立即照常保护，超时仍判输入异常，抬键失败不进入宽限。这不能证明旧日志两次暂停必然由该边界引起，也不能区分用户同时操作相同方向键。

新增 6 项测试覆盖前台等待不撤销挂机、前台仍需空闲、自有抬键滞后、宽限到期、其它键及鼠标/焦点即时保护和修饰键别名；完整 486 项通过。未切窗、发键、改个人配置或重启当前游戏/助手。

## 回位后无遮挡漏检：姓名板半分辨率采样（2026-09-06）

`session-20260906-010008.jsonl` 记录过约 11 秒定位丢失：小地图标记唯一，但姓名板相关分约 0.61、头部约 0.64，均未达到候选阈值；这些帧没有姓名候选可供身份核验。对同一真实截图只改变裁剪原点一像素，半分辨率分数可从 0.86 降到 0.66，而原分辨率保持约 0.94，复现了无遮挡情况下的采样漏检。不能据此认定所有定位丢失只有这一种原因。

姓名板粗检无有效身份时追加同 ROI 的严格原分辨率复核；使用同帧特征缓存，保持字形核验、跨模板身份优先去重、消歧、连续帧及小地图攻击门禁。粗检成功、无模板或已经原分辨率时不重复扫描，不修改个人配置及素材。

修改后另取一帧真实截图，四种裁剪对齐均取得姓名身份分约 0.84；其中两种旧路径得分 0.653/0.697 无候选，修复后达到 0.929。该帧全图正常路径约 14–18 ms，触发复核约 60–65 ms，仅为此帧测量，不代表完整循环耗时或长期 FPS。新增 9 项回归包含四种真实像素合成对齐、ROI、快速路径、错误身份、严格阈值、两帧恢复和歧义拒绝；完整 480 项通过。未切窗、发键或重启用户实例，返程挂机实际效果仍需重启助手验证。

## 混合转向通道修正与前后台切换（2026-09-06）

用户确认从后台切到前台后朝向错误，且之前测试中 PostMessage 转向无效。`session-20260906-005207.jsonl` 中向右回位有真实小地图进展，随后多次记录向左转向及攻击；日志只能证明输入已发送，不能确认实际转身。因此不继续单纯延长等待，按新授权把混合模式转向纳入受保护的前台扫描码通道。

普通攻击、只转向和跳跃攻击都共用限时租约；激活后的新帧重新决定方向，0.06–0.1 秒短按后释放并复核健康状态，再跨帧等待后发技能。前后台切换和新移动租约使朝向记录失效。技能仍为 PostMessage；纯独立后台不变。保留减少反复停攻的修复，动态混合攻击也不每次持有真实方向键。

测试使用真实 Keyboard/HybridMovement 状态机配合模拟桌面与发键 API，验证新帧、目标换边、用户抢焦点、按键失败、租约释放和技能通道隔离；没有向游戏发送测试按键。游戏实际转向仍需重启后验证，不能以模拟通过宣称已确认角色转身。

## 混合模式攻击变慢：过度清除转向状态（2026-09-06）

`session-20260906-005207.jsonl` 确认为混合后台、原地策略，配置攻击间隔 0.22 秒。诊断快照中 129 次攻击伴随 61 次转向准备，反复出现同侧 `previous_direction=null`；多次视觉丢失约 0.3 秒。连续有效帧中的实际发键间隔约 0.27–0.32 秒，但短暂丢失/空目标帧会触发额外停攻与转向，不能把这些间断归因于 PostMessage 本身吞吐量。

0.20.0 的朝向修复在每个上游早退和所有非攻击策略决策后无条件清除转向记录，使识别/选敌抖动放大成反复等待。改为按原因失效：移动、药/Buff、暂停/配置及不可信地图标记仍清除；无目标、原地等待和拾取保留刚发送的转向。视觉空帧只在连续丢失达到 0.5 秒后失效，恢复首帧同样检查完整时长，短暂恢复不延后每 5 秒刷新；原识别与仅地图导航的攻击禁令不变。

回归覆盖短时缺失期间不发攻击、恢复无需额外转向、长时间缺失仍重转、地图歧义停攻、频繁空目标不拖慢同侧攻击，以及换边/移动后的保护。仅模拟输入测试，不改个人配置、不操作游戏；需重启助手验证实际施法节奏。

## 路线拾取短暂定位丢失与阶段计时优化（2026-09-05 23:45）

`session-20260905-231912.jsonl` 的 23:27:27 进入到点拾取，23:27:29 清怪后画面定位丢失，同帧直接进入 returning，约 0.25 秒后恢复却不再继续拾取。此前另两轮也因短暂丢失提前返程，并非用户设置的 7 秒到点停留或 15 秒去程超时届满。

新增可调 1 秒定位等待宽限，短暂丢失先停走/停攻/停拾取，恢复后继续原阶段；丢失超过阈值才转返程，完全无可信位置仍停住，小地图身份与导航总时限不放宽。公共层记录经过药/Buff及定位早退门禁的连续丢失时长，恢复首个决策帧仍能获知缺失时长。去程上限只限制到点前路段；到点有效拾取单独累计，中断暂停、不清零；到点阶段另加默认 60 秒总保护，防止无限清怪。日志带明确 return_reason，阶段/原因变化立即记录，计时进度每秒最多一次。

437 项测试通过，覆盖短暂/持续丢失、恢复首帧、药/Buff跨门禁、小地图缺失、导航到期、15 秒去程加 7 秒到点拾取、实际拾取累计及原参数保存；未操作当前游戏或改写个人配置，需重启助手实测。

## 混合后台恢复原窗口崩溃修复（2026-09-05 23:26）

`logs/crash-20260905-225656.txt` 显示：`HybridMovement.finish()` 尝试恢复旧前台窗口失败，将错误留在输入健康状态中；`BowmanBot.act()` 主体和 finally 的 `check_health()` 先后抛出相同 OSError，异常穿透到面板视觉 worker 并终止线程。这是恢复窗口失败，不是证明移动键无效。

按用户“切到前台后不用回到之前的窗口”的授权，正式混合模式结束、中断及路线分段只释放移动键，不再激活旧窗口；用户自行切走也不抢回。保留激活游戏、唯一实时位置验证、输入隔离、限时抬键、4 秒会话、0.8 秒心跳和用户操作保护。公共行动边界捕获混合输入 OSError，暂停并提示，故障期间暂停发键（含暂停喝药）；需用户检查后显式启动，未释放的移动键禁止重新绑定清除故障。

424 项测试通过，新增异常暂停/连续帧不重报/重新启动/抬键失败保护回归，并更新不恢复旧窗口及分段测试。未重启用户当前实例、未发送游戏按键，完整游戏内效果仍需重启后实测。面板名称现为“混合后台（移动后留前台）”，个人当前输入模式未改动。

## 短暂前台移动与恢复窗口实测（2026-09-05 20:36）

用户授权继续尝试“后台攻击、短暂前台移动”，并确认原挂机退出、右侧安全与暂不操作。先增加独立 `mbv.foreground_move_probe`，默认只读；添加 `--enable-input --confirm-bot-stopped` 才执行一次向右 0.2 秒，不修改个人配置或正式挂机模式。窗口激活使用隔离子进程（3 秒超时），发键前复核前台与进程身份；按住阶段不截图，独立定时抬键并轮询焦点/输入/F9，结束先释放再恢复原窗口。若用户已经切走或原窗口句柄被复用，不继续抢焦点。

实测从 Chrome（HWND 264932，PID 29736）切到 NewMaple（HWND 590382，PID 13796）成功；双方完整性均为 8192。切换前后位置一致，右移前小地图坐标 `(93.5, 34.5)`，0.2 秒 SendInput 后为 `(95.5, 34.5)`，向右位移 **2 像素**，纵向为 0。这验证前台短步可移动，不证明纯后台移动可用。本次没有自动走回原安全点。

但恢复 Chrome 的激活操作未通过验证，报告 `restore_failed`，故完整流程失败，未加入面板或日常策略。之后使用 computer-use 恢复原浏览器也遇到激活失败；刷新后重试检测到用户输入，停止进一步切窗。所有试验键已释放，Python 测试子进程已退出。日志：`logs/foreground-move-probe-20260905-203627-718760.json`。新增 8 项短步限时、失焦释放、恢复目标身份及用户主动切窗回归，完整 **315 项测试通过**。下一步若继续此路线，应先验证只切窗不发键的恢复机制，而不是重复角色移动或直接启用半后台挂机。

## 线程键盘状态共享实测（2026-09-05 20:28）

用户明确授权尝试 `AttachThreadInput + SetKeyboardState`。增加独立 `mbv.shared_state_probe`，由 `movement_probe --shared-key-state --enable-input --confirm-bot-stopped` 调用，不修改正式输入模式或个人配置。先前准备阶段被 F9 占用及游戏前台检查拦截，没有发键；用户退出原助手并手动切到其它窗口后完成本轮。

NewMaple PID 13796、HWND 590382、输入线程 9076；双方完整性级别均为 8192。测试“仅共享状态”和“共享状态加 PostMessage”两种模式，各左右一次，每组 0.3 秒、每 20 毫秒刷新，共四组。每组按下状态均能由附加线程的 GetKeyState 读回，释放状态和解除附加也均成功；这不证明游戏读取了该状态。前台窗口在检查点保持不变、游戏非前台，没有全局 SendInput、焦点伪造、注入或驱动。

四组的唯一小地图标记均为像素 `(15.5, 51.5)`，前后横纵位移均为 0，**没有观察到后台移动**。报告：`logs/movement-probe-20260905-202842-212241.json`。结果支持“仅改变窗口线程键盘状态仍不足以驱动移动”的判断，但不能据此确定 NewMaple 使用 GetAsyncKeyState、DirectInput 或其它线程/焦点检查；既有 PrintWindow 非空缓存帧限制仍适用。未将此实验接入日常挂机，全部测试子进程已退出。

每组使用一次性子进程，父进程不附加输入队列，监控焦点、真实键鼠输入、停止快捷键及超时。正常和异常均尝试清除本组方向状态、抬键与解除附加；先取消等待清理，超时才终止。强制终止后不能保证目标残留状态，需人工检查。附加期间不使用同步 SendMessage，以免共享输入队列使超时失效。新增 14 项状态/清理/子进程回归，完整 **307 项测试通过**。

## 纯窗口剩余兼容对照（2026-09-05 19:00）

新增独立 `mbv.movement_probe`，不改正式输入配置。NewMaple 只有 `NewMapleClass` 主窗口，输入句柄与根句柄均为 590382，无子窗口；探针与游戏完整性级别均为 8192。默认只读，显式启用真实输入时要求停止其它发键器、游戏后台且非最小化；每组限时 0.3 秒、最终抬键，进程身份改变/手动按键/F9/位移/总时限都会停止。

实测 8 种组合 × 左右方向共 16 组：PostMessage 与 SendMessageTimeout（40 毫秒超时）；标准参数、清扩展位参数、每次按下清先前状态位、旧式零参数。游戏在测试采样及发键检查时始终为后台，唯一小地图标记始终为像素 `(112.5, 47.5)`；所有组横纵位移均为 0，API 未报错。结果报告：`logs/movement-probe-20260905-190026-167638.json`。没有发现可用的纯窗口移动组合，未推广任何参数到日常挂机。探针已自行释放按键并退出。共 **291 项测试通过**，新增 9 项覆盖参数矩阵、同步超时、异常抬键、目标身份及输入限时。

## 移动修复与实测补充（2026-09-05 18:47）

原地策略已改为平时超出小地图安全点容差也优先回位。短步改为跨帧停攻准备、限时按键、位移验证；只在正确方向实际位移后更新计时和待回位标志。三轮无位移或反向位移暂停，普通回位连续 2 秒无进展重试、4 秒无进展暂停。输入层独立计时抬键，后台重复发键错误会交给主循环；没有切换成全局输入或伪造焦点。

新增移动回归后共 **282 项测试通过**。真实 NewMaple 测试中，游戏失焦、唯一小地图标记保持 `(0.790598, 0.415663)`，右移脉冲连续尝试 0.12/0.24/0.36 秒，测得位移均为零；日志 `session-20260905-184347.jsonl` 在第三次后记录 `movement_failed` 并暂停。故**策略与失败保护已修复，但纯窗口后台移动仍未解决**，不能将消息投递或单元测试成功等同于游戏移动成功。测试实例已通过 F9 退出；原控制面板进程仍在，但其视觉/输入会话已结束，需要用户关闭面板并重新启动。

日期：2026-09-05。对象是当前本地工作区，包含之前尚未提交的 NewMaple、玩家识别、策略和 Buff 修改。此次审查覆盖启动/档案、配置与素材、采集、视觉跟踪、三种策略、输入调度、Tk/HUD、性能监控及测试。以下是发现的问题，不代表不存在其它缺陷。

后续修复状态（2026-09-05）：用户已实测确认独立后台可挂机。本次优化完成下列第 1–6 项，第 7 项整帧配置快照及性能优化仍待处理。历史问题描述保留供追溯，代码链接的行号以最初审查时为准。

- 连续帧确认使用帧编号去重，空帧/歧义打断确认链；暂停和真实视觉记录清理旧待确认状态。
- Buff 的 0.45 秒停攻准备改为跨帧推进，发键前检查暂停/退出/窗口/前台；后台模式允许失焦。按键保持仍使用现有 0.18 秒 tap，后续可再把该阶段改成可中断状态机。
- 标飞安全区上下边界均使用小地图坐标。
- 线程异常由 Tk 主线程轮询显示，去掉会捕获已清除异常变量的延后 lambda。
- Buff 按最早到期时间调度，新增短间隔三槽位公平性回归。
- 配置采用同目录临时文件、flush/fsync、原子替换及上一版 `.bak` 备份；多实例并发覆盖仍未解决。

本次完整测试 **263 项通过**，新增 14 项可靠性回归，保留后台输入隔离与真实 Windows 消息窗口测试。未重启或改动正在运行的用户挂机会话，新代码需重启助手生效。

## 建议优先处理的缺陷

### 1. [P1] 玩家重获的“连续帧”会跨越无匹配帧累计

位置：[player_tracking.py](D:/CodexSpace/MapleBowmanVision/mbv/player_tracking.py:257)、[bot.py](D:/CodexSpace/MapleBowmanVision/mbv/bot.py:1516)。

`consider_reacquisition()` 累计 `pending_count`，但没有有效候选时不会重置；`mark_miss()` 也保留计数。实测第一次候选返回 None，调用 50 次 `mark_miss()` 后，同位置的第二个候选立即确认。实际调用者在没有候选的帧中正好走这条路径，所以“两帧确认”可以由相隔很久的两次命中满足，弱姓名板恢复和辅助认人都受影响。

建议按帧编号或连续候选状态计数；本帧无候选、候选歧义或确认类型变化时清空计数。不能直接在所有 `mark_miss()` 中无条件清零，因为合法的首个待确认帧也会调用它。增加“命中→空帧→命中仍不得确认”的回归用例。

### 2. [P1] Buff 停攻等待期间失焦，前台模式仍可能发键

位置：[bot.py](D:/CodexSpace/MapleBowmanVision/mbv/bot.py:583)。

主执行器进入 `_act()` 时检查前台，然后 `_try_auto_buff()` 等待 0.45 秒，再按键 0.18 秒；等待后没有再次检查前台或退出请求。如果这段时间用户切到其它窗口，前台模式的全局输入仍可能发到新窗口；F8/F9 请求也要等当前动作结束才处理。

建议将施法准备、按下、抬起、保护窗口改为可中断的分阶段调度，并在每次实际 keydown 前检查授权、暂停、窗口与前台条件。此次独立窗口消息模式避免向其它应用投递按键，但原前台/兼容输入流程仍需优化。

### 3. [P2] 标飞安全区混用了主画面和小地图的 Y 坐标

位置：[throwing_star.py](D:/CodexSpace/MapleBowmanVision/mbv/strategies/thief/throwing_star.py:360)。

`top` 来自小地图安全区，但 `if player_y < top` 使用主画面角色高度；同分支下方检查已经使用 `safe_player_y`。镜头移动或两种画面布局不同，就可能在角色实际位于安全区内时一直返回 `SAFE_OUTPUT_ABOVE`，或者漏掉角色高于安全区的情况。

建议统一用小地图 `safe_player_y` 比较，测试刻意让主画面与小地图 Y 不同，避免现有同数值样例掩盖错误。

### 4. [P2] 视觉线程报错后的 UI 回调可能再次报错

位置：[panel.py](D:/CodexSpace/MapleBowmanVision/mbv/panel.py:213)。

`except BaseException as exc` 内排队 `lambda: self._worker_failed(exc)`。Python 退出 except 后会清除异常变量，延后运行的 lambda 可能因找不到 `exc` 报 NameError，遮盖真正错误并阻断后续关闭流程。

建议绑定回调参数，或者把异常对象放到线程安全队列、由 Tk 主线程轮询显示。后者也能减少 worker 对 `root.after` 的跨线程依赖。

### 5. [P2] Buff 固定槽位优先级可能让后面两项永远不执行

位置：[buffs.py](D:/CodexSpace/MapleBowmanVision/mbv/buffs.py:38)。

调度总从 Buff 1 开始，施法保护为 1.2 秒。当 Buff 1 的间隔比实际调度周期短，它每次都是已到期项，Buff 2/3 无法获选。实测三项都设 1 秒，连续五次调度结果全是 `buff_1`。面板允许用户直接输入该间隔。

建议按最早到期时间或逾期程度选项，同到期轮转；也可在 UI 提示小于施法耗时的间隔无法满足。保留独立开关和原有冷却时间。

### 6. [P2] 保存配置先截断原文件，中断时可能丢失校准数据

位置：[config.py](D:/CodexSpace/MapleBowmanVision/mbv/config.py:331)。

`save_config()` 直接以 `w` 打开个人配置，再序列化。写入途中出错、程序退出或其它实例同时读写时，可能留下半个 JSON，下一次启动也无法加载。配置包含用户花时间采集的坐标、按键和素材分类选择，不能只依赖再次采集恢复。

建议先在同目录写临时文件、完成 flush/fsync 后原子替换，保留上一份可用备份；若允许多个实例同时运行，再增加写入版本校验。原子替换解决半文件，不独自解决并发覆盖。

### 7. [P2] 配置热更新不能保证一帧使用同一套配置

位置：[bot.py](D:/CodexSpace/MapleBowmanVision/mbv/bot.py:698)、[bot.py](D:/CodexSpace/MapleBowmanVision/mbv/bot.py:1783)。

`apply_config()` 替换配置、策略和 tracker 时有锁，但视觉循环没有使用该锁获取完整快照。循环先保留旧 `vision`，随后又多次读取当前 `self.config` 的区域、策略和参数；更新恰好发生在帧中间时，可能混用旧阈值与新区块，并留下旧检测结果。更新时暂停减少了即时发键风险，但不保证识别状态一致。

建议每帧取得配置/模板/策略的不可变快照和版本号；配置版本改变后丢弃旧帧结果。无需把整个 OpenCV 过程锁住，否则会卡面板。

以上为最初审查时的缺陷记录；第 1–6 项已在后续优化中处理，当前状态见文档开头。

## 性能与结构优化

- 玩家丢失时每帧全图扫描姓名板、头部、称号，随后又可能局部低阈值扫描。可以持续进行小范围恢复，低频全图搜索；先根据现有分段耗时确定预算，保留周期全图校验。
- `nameplate_identity_similarity()` 为每个候选重新生成模板字形掩码和像素统计。模板部分可像缩放特征一样缓存，候选只计算现场部分。多模板字形匹配和真实失败截图回放比继续堆阈值更值得投入。
- `bot.py` 约 2200 行、`panel.py` 约 2600 行，配置迁移、视觉追踪和动作状态互相交织。建议先提取独立 PlayerTracker、FrameSnapshot 与动作调度器，再拆面板卡片；不建议先做纯文件搬家。
- 模板 Alpha、同帧 SceneFeatures 缓存、策略注册表、模板回收及已有性能快照设计可以继续保留。没有对算法提速做基准测试，本轮不宣称具体 CPU/FPS 收益。

## 本轮已实现的后台实验

- 新增 `input.delivery=window_message`；面板选择“独立后台（实验）”即会持久化并应用。沿用当前档案校准和素材，旧模式保持可选。
- 只向一个解析后的游戏输入 HWND 投递 `WM_KEYDOWN/WM_KEYUP`，不调用 SendInput，不附加 WM_CHAR、同步重复按键消息或伪造焦点事件。Windows 消息投递成功不代表客户端实际接受技能。
- 新增 PrintWindow 客户区截图。辅助进程启动等待最多 5 秒，单次截图等待 0.8 秒；失败、空白、窗口最小化或超时会暂停，无桌面截图回退。辅助进程随退出/切换模式清理。
- 独立模式不主动切前台或置顶；失焦时隐藏游戏 HUD，避免覆盖用户当前应用。最小化和客户端停止后台绘制仍不支持；非空缓存帧尚不能可靠判定为冻结。
- 修复共用输入层的两个缺陷：前台 `release_all()` 曾未给 `held` 中的键发送 keyup；重复发键线程曾在锁外 dispatch，可能在 up 后再次 down。现在按住集合与投递顺序使用同一锁，停止重复后再释放。
- 增加只读 `python -m mbv.background_probe --profile newmaple --samples 3`，输出截图耗时和姓名板分数，不向游戏发键。

## 验证结果及边界

- 项目虚拟环境完整测试 **249 项通过**，新增 10 项验证覆盖窗口消息隔离、无全局回退、暂停释放、重复线程停止、截图超时/错误/复用、不抢焦点及不置顶。
- 使用真实 Windows 不可见消息窗口做集成测试：收到了 right 的 KEYDOWN/KEYUP，前台焦点不变，SendInput 没有被调用。这个测试窗口属于测试进程，不是 NewMaple。
- 对当前 NewMaple 实测 PrintWindow 返回 1280×720 图片，热启动后截图约 34–45 ms。一次采样姓名板 0.7069、身份分约 0.84；后一次采样原始分约 0.54、无有效身份候选。仅凭这些分数不能确定后一次低分的原因，也不能宣称后台识别稳定。
- 测试时 NewMaple 位于前台且旧助手仍在发攻击键，因此没有叠加实机按键、切焦点或遮挡测试。**NewMaple 是否在失焦后接受消息、持续渲染和正确补 Buff，仍待游戏内验证。** 本轮保留个人当前输入模式，没有自动切换运行中的会话。
- PrintWindow 由目标程序处理，且是同步调用；窗口消息与异步键盘状态也是不同机制。参考 [Microsoft PrintWindow 文档](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-printwindow)、[Microsoft 键盘输入说明](https://learn.microsoft.com/en-us/windows/win32/learnwin32/keyboard-input)。

后续优先处理整帧配置一致性，再针对实测瓶颈调整视觉扫描频率和模板字形缓存。后台运行已获用户实测确认，仍保留最小化和画面冻结等已知边界。
