# 更新日志

版本号在 `mbv/__init__.py` 的 `__version__`。每次提交必须同时改版本号并在本文件追加条目。

格式：主版本.次版本.补丁（破坏性变更 / 新功能 / 修复）。

## 0.20.0 — 2026-09-06

### 混合后台移动与异常保护

- 新增显式选择的 `hybrid` 模式：实际移动、回位跳跃、周期短步和找人位移使用独立的前台 SendInput 通道；攻击、转向、近身技能、Buff、喝药及拾取仍走 PostMessage，不改变纯独立后台的输入隔离承诺。
- 移动前等待键鼠空闲 0.7 秒，验证游戏窗口与进程身份，激活后等待新视觉帧；窗口激活放在限时子进程，输入具备会话、按键续期、行动心跳及独立 watchdog 保护。路线分段保留移动无进展监控。
- 修复移动结束恢复旧窗口失败导致视觉线程退出的问题。按用户授权改为“混合后台（移动后留前台）”：结束、中断、暂停只抬键，不再自动恢复原窗口；用户自行切走时不抢回。
- 混合输入异常在公共行动边界暂停并提示，不再穿透到 worker；故障期间不继续暂停喝药，须显式重新启动并绑定，移动键未释放时禁止清除错误继续运行。

### 原地定时路线拾取

- 原地策略新增可开关的同平台路线拾取，目标点像安全点一样在冻结放大的小地图中点选；默认间隔 180 秒，采样与安全点保持独立，重采小地图会使旧点位失效。
- 去程沿路拾取，遇有效怪物先停下清怪（支持近身技能）；到点拾取结束后返回安全点，返程优先回位、不攻击，回到安全点再恢复输出。关闭功能时结束拾取并回位，暂停立即释放按键。
- 区分“到点前最长秒”（默认 90 秒）和“到点有效拾取秒”（默认 1.5 秒）；有效拾取单独累计，清怪、药/Buff、定位中断只暂停累计、不清零，未实际发送拾取键不计时。
- 增加默认 1 秒的短暂定位丢失宽限，先停下等待恢复，超过才转返程；完全无可信位置仍停止。到点阶段另有默认 60 秒总上限，防止无限清怪；相关秒数可直接输入，并保留用户已有数值。
- 记录明确返程原因及限频进度；确认实际离开并回到安全点后，重新计算拾取间隔及 45 秒周期右移，完成一次拾取往返可替代本轮右移。

### 原地近身技能与攻击朝向

- 原地策略新增独立近身技能采集，参考弓箭手动态共用距离计算；按怪物检测框宽度归一化，默认进入/退出阈值为 0.35/0.9 倍，支持调整，清空按键即关闭，远离后恢复通用攻击键。
- 修复原地攻击只相信缓存朝向的问题：首次输出、换边和移动/药/Buff/定位中断后，距上次攻击至少 0.6 秒再独立短按方向至少 0.06 秒，等待 0.08 秒并在后续帧复核目标后攻击。
- 同侧持续输出每 5 秒重发转向，避免一次方向键被技能动作吞掉后长期打反；不在每次攻击时持续按方向，保留原输入模式与自定义较长转向按键时长。
- 转向准备期间显示转向状态，攻击日志明确 `facing_verified=false`：HUD 和日志的攻击方向是输入意图，不是角色实际朝向的视觉确认。

### 小地图遮挡补位与主导航

- 在既有姓名板身份与视觉/唯一小地图标记配对基础上，新增固定背景纹理校验；静止标记位移不超过 0.5 像素且背景稳定时，允许默认 3 秒（最多 5 秒）的遮挡补位。
- 保留旧 0.2 秒补位规则；延长补位不刷新身份、视觉时间、速度或固定基准。移动、多候选、背景变化、证据不足或超时立即失效，不允许移动后回原点复活旧定位。
- 仅剩可信小地图位置时，支持默认 10 秒（最多 30 秒）的主导航回安全点/安全区；清空屏幕目标，只允许安全导航和等待，禁止攻击、追怪、拾取及新周期短步。到期停止，不盲目找人。
- 公共策略接口增加地图导航限制、会话运行状态和中断信息；路线状态归运行会话所有，不放入全局策略实例。面板新增点位采集和实时开关元数据，配置与 NewMaple 示例同步。

### 独立诊断与验证

- 增加显式授权的线程键盘状态共享探针和前台短步探针：默认不发键，真实测试需确认其它发键器已退出，使用隔离子进程、限时释放、窗口身份和用户操作检查，不接入日常策略。
- 保留实测结论及失败记录：线程状态共享未观察到后台位移，前台短步观察到位移但恢复旧窗口失败；不能把 API 成功当作游戏接受移动，也不宣称纯后台移动已解决。
- 更新 README、策略扩展约定及代码审查记录；完整回归测试 **447 项通过**，覆盖混合输入安全、地图定位边界、路线计时/中断、近身技能和转向时序。
- 本次未重新进行游戏内实测；混合模式仍会短暂影响前台，方向短按可能造成微小位移，独立后台仍不支持最小化。个人配置、采样图片、素材分类占位目录和运行日志不提交。

## 0.19.0 — 2026-09-05

### NewMaple 客户端与独立档案

- `Start.bat` 默认启动 `newmaple` 档案，按 `NewMaple.exe` / `NewMaple` 识别桌面客户端；`Start.bat classic` 保留原怀旧服入口，CLI 支持显式选择档案。
- 新增 `profiles/newmaple/config.example.json` 和独立素材目录；校准、五类模板加载/采集/管理/回收、分类统计及面板均按当前档案解析路径，避免复用 classic 的个人数据。
- 改进启动参数、面板档案标识与异常处理；个人配置、配置备份、采集图片和运行日志不纳入本次提交。

### 全局自动喝药与三组定时 Buff

- 自动喝药开关改为真正的会话级全局开关：关闭时，挂机与暂停状态都不发送 HP/MP 药键；开启后，暂停状态仅在游戏位于前台时补药。
- 新增三组 Buff，分别支持按键采集、秒数直接输入及独立持久化开关；关闭单项保留按键、间隔和上次发送时间，空按键或零间隔同样禁用。
- Buff 仅在挂机运行时调度；暂停恢复不重置冷却，同到期每帧最多一个，按最早到期顺序避免短间隔槽位长期抢占。
- 施法采用跨帧 0.45 秒停攻准备、0.18 秒按键保持及 1.2 秒保护；准备期间可取消，发键前复核窗口/退出/暂停及前台要求，独立后台模式允许失焦补 Buff。
- 所有下拉框禁止滚轮改值，降低滚动控制面板时误改职业、策略、分类和输入模式的风险。

### 玩家识别、技能选择与回位

- 姓名板身份已建立后，允许在受限预测位置使用较低阈值恢复；头部/称号补位保留距离、身份、时间与连续帧约束，不放宽首次全图认人规则。
- 连续重获确认按实际视觉帧计数，空帧/歧义打断确认链，同帧重复扫描不增加帧数；增加丢失/重获日志，改善动画、遮挡后的定位续认。
- 小地图颜色相近的校准提示结合实时唯一性判断，避免仍可正常识别时被不必要地阻止启动。
- 弓箭手动态策略新增 AOE、单体和近身技能采集：聚怪数量/距离决定 AOE，贴身使用近身技能；距离按怪物检测框体型归一化，进入/退出阈值分离并可调整。
- 动态攻击换边先准备转向并等待下一帧确认，再发送技能；保留独立转向与方向键异常释放，减少提示朝向和实际攻击朝向不一致。
- 原地攻击平时偏离小地图安全点也优先回位；默认每 45 秒停攻后向右小步，采用跨帧准备、输入层限时抬键和实际位移验证，不再把发键当作移动完成。
- 小步最多三轮尝试，正确方向位移才更新周期计时；普通回位连续 2 秒无进展重试、4 秒无进展暂停，记录移动开始/进展/重试/失败。补药、Buff、暂停与识别中断安全释放移动键。
- 修复标飞安全区 Y 判断混用战斗画面与小地图坐标；保留远程 0.18.0 的近目标跳跃攻击和默认关闭安全区巡逻行为，并同步 NewMaple 示例配置。

### 独立后台实验与稳定性

- 新增 `window_message` 模式：只向绑定的游戏输入窗口发送消息，不回退全局 SendInput，不伪造焦点；旧 `background`/`postmessage`/`window` 兼容扫描码含义保持不变。
- PrintWindow 截图隔离在可终止的辅助进程；启动/截图设置超时，失败、空白或最小化暂停，不回退桌面截图。独立后台不切前台、不置顶，失焦时隐藏 HUD。
- 修复暂停时实际抬键和重复线程竞态；移动消息增加抬按脉冲尝试、独立限时释放，重复投递错误交回主循环处理。
- 配置使用同目录临时文件、flush/fsync 与原子替换，保留上一份可解析的 `.bak`；视觉线程异常由 Tk 主线程消费，避免延迟回调丢失异常变量。
- 新增只读截图诊断与显式授权的独立移动兼容探针，覆盖同步/异步消息、参数、重复状态位和左右方向；默认不发键，实测限时并检查焦点、目标身份和停止条件。

### 验证与已知限制

- 合入远程 0.18.0 后完整回归测试 **293 项通过**，覆盖档案隔离、全局喝药、Buff 调度、识别恢复、技能分流、回位及后台输入/截图安全。
- **NewMaple 独立后台攻击已获实测确认，但后台移动仍未解决**：右移脉冲三轮无位移；剩余 16 组纯窗口兼容对照亦均无位移，即使 API 调用成功也不能认为游戏接受移动。未将失败的试验参数推广到正式输入配置。
- 独立后台不支持最小化；非空缓存画面尚不能可靠判为冻结。整帧配置快照一致性、多实例配置覆盖与进一步性能优化仍待处理，详见 `CODE_REVIEW.md`。

## 0.18.0 — 2026-08-16

- 标飞新增可调的近目标每次跳跃攻击：目标进入设定水平距离后，每次攻击优先执行跳跃攻击，并沿用公共攻击间隔。
- 标飞安全输出区内无目标巡逻改为默认关闭，可在策略设置中独立开启；补充相关配置、面板说明和回归测试。

## 0.17.0 — 2026-08-15

- 标飞新增多个独立索敌区：区域跟随稳定角色锚点移动、保持屏幕方向且不随面向翻转，支持区域优先级、最近距离或识别分选敌，并可选叠加通用索敌框。
- 标飞安全输出区改为在放大的小地图上框选，回位和无目标巡逻统一使用小地图玩家标记，战斗镜头滚动不再导致安全区漂移；旧战斗画面安全区自动失效并要求重采。
- 新增索敌成功后只改变面向的动作，以及安全区内无目标左右巡逻、边缘折返和可调巡逻边距；玩家姓名板超时丢失时可按可调时长交替左右位移恢复。
- Debug 框改为默认全部显示、各采集项独立开关，面板按钮同步显示开关状态；标飞安全区显示在小地图上，多索敌区继续显示在战斗画面。
- 修复日常启动闪退与 Python 环境检测流程，保留 `Start.bat` 为唯一日常入口；扩展配置迁移、采集、策略、输入安全和界面回归测试至 209 项。

## 0.16.0 — 2026-08-15

- 弓箭手动态的小地图平台安全点改为二维回位：水平越界优先左右移动，掉到下层时按间隔向上跳回，位于上层时下跳；新增水平/垂直容差、跳跃间隔和下方向键配置。
- 弓箭手攻击与追踪目标改为按玩家稳定攻击锚点到怪物中心的二维实际距离排序，优先处理最近单位；原地攻击和标飞策略保持原有排序。
- 弓箭手每次攻击都重新计算目标方向，先按住方向键完成转向，在方向键仍按下时发出攻击再释放，减少被怪物击退后朝向错乱；异常时也会安全释放方向键。
- 更新策略说明、面板和示例配置，回归测试扩展至 194 项。

## 0.15.0 — 2026-08-15

- 新增小地图静止确认：可靠姓名板命中，以及姓名板身份建立后的连续头部命中，会固定记录同帧本人标记；三路屏幕定位随后暂时丢失时，标记在 2 像素内保持静止即可在最多 0.2 秒内沿用上次视觉位置。
- 静止确认不会刷新视觉或身份时间，也不会逐帧滚动续命；标记移动、缺失、多重命中、尺寸变化或超时会立即清除位置并停止动作，移动后必须重新取得视觉命中才能建立新基准。
- 控制面板新增可自动保存的静止判断开关，HUD 用橙色虚线玩家框区分推断位置，并补充固定基准、像素边界、头部刷新、身份来源和动作清理回归测试。

## 0.14.0 — 2026-08-15

- 控制面板顶部新增可折叠性能监控卡，每秒显示实际 / 目标 FPS、整帧平均与 P95、超预算比例、进程 CPU 和内存；折叠状态会自动保存，旧配置会补齐默认值。
- 视觉主循环新增低开销分段计时，分别统计截图、预处理、怪物识别、玩家定位、策略选敌、决策输入和 HUD 准备；整帧耗时不包含帧率节流等待，独立喝药模式会明确省略未执行的识别阶段。
- 新增线程安全的有界滚动统计器，覆盖截图失败、采集暂停、恢复、视觉线程异常与正常退出状态，并补充确定性 P95、资源采样、并发读写和旧配置兼容回归测试。

## 0.13.0 — 2026-08-14

- 重构玩家遮挡定位：姓名板改为按 Alpha 有效字形区验证并按身份去重，无效的姓名板原始候选不再阻断头部/称号补位；已确认身份后可持续局部跟踪，启动时也可由高置信辅助模板连续 3 帧建立受限身份。
- 小地图玩家标记采集改为最高 8 倍放大点选并映射回原始冻结帧，通过连通高亮色块与唯一性校验生成 HSV 条件，同时保存初始归一化位置供首帧消歧。
- 将弓箭手的“战斗画面平台中心”迁移为“小地图平台安全点”：支持放大采集、显式坐标系与旧配置失效迁移，弓箭手动态策略按小地图位置优先回到安全范围，且运行 HUD 不显示安全点。
- 重做怪物模板生成：基于选区边缘估计背景、提取最大主体 Alpha、自动紧裁并拒绝无法区分的全帧结果；检测新增可配置的轮廓结构权重，减少棕色地形与空气误框。
- 删除仓库内旧的初始姓名板、头部和称号模板，更新控制面板、CLI、弓箭手策略规范、示例配置与使用文档；回归测试扩展至 156 项。

## 0.12.0 — 2026-08-14

- 玩家定位拆分为“名字字形确认身份”和“头部/称号短时补位”：姓名板候选在原分辨率验证唯一名字，遮挡时不再用其他玩家的头部或公共称号建立新身份，远端重获需连续确认后才恢复动作。
- 新采集的姓名板、头部和称号会在同一冻结帧继续采集角色脚底点，并为每张模板保存独立脚底偏移；旧模板继续兼容原有固定偏移，重新采集后可消除不同裁剪范围造成的定位跳动。
- 修复“原地攻击”沿用当前面向的非对称索敌区、导致左侧有怪时仍持续朝右攻击的问题；该策略现在使用左右两个面向索敌区的并集选择最近目标。

## 0.11.0 — 2026-08-14

- 玩家识别改为预测位置局部搜索：稳定时复用统一缩放网格，只扫描角色附近；连续丢失或周期复核时恢复全图姓名板、头部、称号三路融合，并支持传送、换层后的远端安全重获。
- 自动喝药拆为独立控制器，面板新增默认关闭的会话开关；暂停挂机时可单独监测血蓝，非挂机状态仅在游戏位于前台时发药键，并跳过战斗模板匹配以降低 CPU 占用。
- 加强补药模式切换、采集暂停、窗口/截图失效和异常按键释放的输入安全；测试扩展到 129 项，并补齐多缩放 ROI、预测候选、旧锚隔离、三路投票及补药安全门回归。

## 0.10.0 — 2026-08-14

- 控制面板删除游戏窗口解耦后已失去作用的右侧说明与实时摘要，收窄为单栏工具；采集、校验、模板管理、职业策略、参数、Debug、保存和启停功能全部保留。
- “挂机时强制游戏窗口置顶”改为可自动保存的选项：默认保持开启；关闭且使用后台按键时，启动挂机不再抢占前台或置顶游戏窗口。
- 更新使用手册中的独立窗口、标飞策略、后台按键和可选置顶说明；测试扩展到 107 项，覆盖关闭置顶后的后台启动路径与单栏控制面板。

## 0.9.0 — 2026-08-14

- 修正采集项“显示”的 Debug 逻辑：行内按钮现在只显示当前采集项，不再误开全部识别框；F7 和底部 Debug 开关仍负责全部显示或全部隐藏。
- 为血条、蓝条、小地图、玩家标记、战斗区、平台中心、通用索敌范围和玩家姓名板建立单项 Debug 过滤，并补上平台中心的十字标记。
- 玩家姓名板从“战斗识别”移入“模板采集”；模板采集和图片管理器均按“怪物模板 / 人物模板”两个子类分组，原有采集、预览、删除、分类和数量统计保持不变。

## 0.8.0 — 2026-08-14

- 工具窗口与游戏窗口完全解耦：移除真实游戏窗口停靠、移动、缩放和退出恢复逻辑，控制台改为独立可移动工具窗口。
- 采集框选层和 Debug 识别框仍直接显示在独立游戏窗口上；挂机时的游戏置顶、暂停恢复、后台按键、采集、策略和实时状态功能保持不变。
- 控制台恢复为适合与游戏并排摆放的常规尺寸，右侧明确显示独立窗口模式、采集方式和实时识别状态。

## 0.7.1 — 2026-08-14

- 修复独立采集时游戏窗口在冻结帧上层打开后又被控制台停靠线程改尺寸，导致下一项采集将上一项误判为失效的问题；现在采集前先稳定停靠尺寸，采集期间禁止重新停靠。
- 血条、蓝条等按归一化坐标保存的项目在等比缩放后继续有效；仅当游戏客户区宽高比变化超过 3% 时，才要求重新校准。

## 0.7.0 — 2026-08-14

- 控制面板重构为采集控制台：左侧保留全部采集、怪物分类、模板管理、职业策略和参数入口，中间停靠真实游戏窗口，下方显示实时识别状态，底部集中保存、Debug、启动/暂停和退出操作。
- 游戏窗口采用独立 HWND 停靠而非 `SetParent` 嵌入；仅在助手权限足够时移动，退出后恢复原始窗口状态，挂机期间的强制置顶与暂停恢复规则保持不变。
- 血条、蓝条、小地图、小地图玩家标记、战斗识别区和平台中心改为单项采集、单项保存；重采小地图或战斗区时只使对应依赖项失效，旧组合采集入口继续兼容。
- 职业与基础策略分层选择，策略布尔模块使用多选；标飞安全输出区保持可选，新增可关闭的近身重叠跳跃攻击、默认 20% 水平重叠阈值和 Alt→Shift 跳攻时序。
- 新增窗口尺寸校验：停靠后尺寸与上次校准不一致时阻止启动并提示逐项重采，避免旧坐标在新客户区中被误判为有效。

## 0.6.0 — 2026-08-14

- 新增“标飞安全输出”职业策略及专属二维安全输出区框选：玩家低于安全区时优先按住水平回位方向并周期性向上跳，进入安全区后才允许输出。
- 标飞只选择角色下方且位于通用索敌区内的怪物，同层目标会被过滤；默认不追怪、不巡逻、不自动拾取，玩家位于安全区上方的下跳回位暂不处理。
- 新增可配置跳跃键、回位跳跃间隔、目标最小向下高度差，以及 F7 Debug 框中的标飞安全区显示；旧配置会安全补齐默认值但保持“尚未框选”状态。

## 0.5.1 — 2026-08-14

- 修复首次启动缺少个人 `config.json` 时，隐藏控制台直接退出的问题；现在会自动从示例配置创建一次，且绝不覆盖已有个人配置。

## 0.5.0 — 2026-08-14

- 启动挂机后自动将游戏窗口设为置顶，避免普通窗口覆盖视觉识别区；暂停、安全停机或退出时自动取消置顶，并在置顶失败时阻止启动且显示原因。

## 0.4.0 — 2026-08-14

- 新增统一的采集图片管理器：怪物模板、过滤项、姓名板、头部和称号勋章均可按类型查看数量、预览原图、继续新增采集，并单张移入项目内的模板回收目录。
- 删除任意类型图片后立即重载运行时识别模板、刷新统计并选中相邻图片；删除最后一张姓名板前会明确提示必须重新采集才能启动挂机。
- 五类模板共用带路径边界检查的列表与可恢复删除接口；玩家三类文件名改用微秒时间戳，避免同一秒连续采集覆盖旧图。

## 0.3.0 — 2026-08-13

- 将战斗识别区与平台中心采集从“状态栏与小地图校准”中独立出来；两步采集复用同一冻结帧，平台中心按战斗区归一化保存，并只作为策略数据、不在 HUD 中显示。
- 新增可扩展职业策略注册体系、策略下拉框、实时逻辑说明和 `STRATEGIES.md` 协作规范；策略配置统一存放在 `strategy.options.<strategy_key>`，公共安全、补药和按键执行继续由运行层负责。
- 默认“弓箭手动态”策略增加平台中心安全半径：角色离平台中心过远时优先回位，再执行区内攻击、同层追踪及可选拾取/巡逻。
- 新增“原地攻击”通用策略：只在公共索敌区内选择左右目标并攻击，不追怪、不巡逻、不回平台中心；同侧连续攻击不重复点方向键，仅换边时转向以减少位置漂移。
- 将弓箭手专属攻击框迁移为所有策略共享的 `targeting.box`，兼容旧配置一次性迁移；默认索敌框总高度提高 20%、总宽度缩短 10%，面板支持直接微调、框选、运行时预览和重启持久化。
- 统一姓名板、头部和称号三种玩家定位来源的脚底攻击锚点，并增加轻量 EMA 平滑与大幅移动快速跟随，解决索敌框随检测来源上下跳动的问题。
- 挂机配置改为自动持久化：策略切换及参数、通用索敌区、数值微调、HP/MP 滑杆和复选框均会在运行时保存；退出前再次完整落盘，最长运行时间默认 `0`（不限时）。
- 更新控制面板、CLI、安装提示、README 和兼容导出；测试扩展到 77 项，覆盖校准拆分、配置迁移与持久化、稳定锚点、策略注册/选敌/动作优先级及原地转向行为。

## 0.2.0 — 2026-08-13

- 怪物模板支持分类、新建、重命名、可恢复删除和图片预览；分类目录带可跟踪占位文件，可随 Git 上传，删除单张模板后管理窗口保持打开并自动选择下一张。
- 新增按分类保存的怪物过滤项，只抑制同分类、同位置的误识别候选；当前只加载面板选中的怪物分类，切换后立即生效并持久化。
- 日常启动入口统一为 `Start.bat`；启动时完成 UAC 并进入输入待命，但仍需手动启动挂机，旧启动与采集批处理入口移除。
- 面板新增与 F7 双向同步的 Debug 框开关；HP、MP 自动喝药阈值改为 0～100% 滑杆及 1% 微调按钮。
- 修复采集提示被运行 HUD 覆盖、全局热键缺少按键映射导入，以及保存配置或打开采集工具时可能遗留一次输入的竞态。
- 怪物分类、过滤项和模板回收目录改为递归忽略；移除仓库中误跟踪的个人怪物模板图片。
- 测试扩展到 60 项，覆盖分类存储、过滤抑制、图片预览、单入口、Debug 开关、阈值保存及输入安全。

## 0.1.3 — 2026-08-13

- 文档：写明 Cursor 与本机 Codex 共用 `AGENTS.md`，并补上本仓库 git 身份与并行开发约定。
- 性能：姓名板稳定定位时将头部、称号检测降为每 0.5 秒复核；姓名板失败、定位过期或模板重载后仍立即恢复三路检测。
- 稳定性：玩家定位保持时间到期后清除旧位置先验，允许传送或长时间遮挡后重新定位。
- 测试：改用可提交的示例配置并移除被同名方法覆盖的重复用例，恢复干净工作区下的完整测试。

## 0.1.2 — 2026-08-13

- 性能：同一帧的怪物、姓名板、头部、称号检测共用一份场景特征（缩放 / 颜色对立通道 / 边缘只算一次）；模板侧特征按缩放比例缓存，识别结果不变。
- 架构：控制面板改为直接从 `mbv` 各模块导入，不再依赖根目录兼容入口。

## 0.1.1 — 2026-08-13

- 去掉已废弃的 OpenCV 校准窗口、预览常量，以及未再使用的玩家检测选择函数。
- 攻击目标筛选共用同一套距离排序；兼容入口只再导出测试和面板真正用到的符号。
- `Start-Observer.bat` 改为调用 `Start.bat`，避免两份脚本各自改丢。

## 0.1.0 — 2026-08-13

- 代码拆到 `mbv/` 包，根目录只保留启动入口。
- 控制面板统一采集、观察与挂机配置。
- 后台按键始终发送扫描码；攻击范围按角色中心和面向框选。
- 模板采集改为冻结当前画面再框选。
- F7 显隐校对 HUD。
