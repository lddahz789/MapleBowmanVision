# 更新日志

版本号在 `mbv/__init__.py` 的 `__version__`。每次提交必须同时改版本号并在本文件追加条目。

格式：主版本.次版本.补丁（破坏性变更 / 新功能 / 修复）。

## 0.30.0 — 2026-09-16

- 职业策略的龙咆哮“施放间隔”改为毫秒显示和输入（100–10000 毫秒，默认 1000 毫秒）；微调、直接输入、保存、重载和切换策略统一换算，内部及原配置仍用秒，不改变原有实际间隔。
- 随仓库附带精简怪物模板库：1,652 个怪物记录、18,172 个动作帧引用，按内容去重为 8,262 张原始 PNG，图片约 103 MiB。仅包含站立、移动、飞行动作及查询必要索引，不上传地图、角色、装备、音效、攻击/死亡特效或完整解包数据库。
- 模板查询默认使用项目内 `resources/monster_library`，无需本机 D 盘解包目录；默认设置保存为可随项目搬迁的相对路径，旧默认目录自动迁移，其他自选资源目录保留。
- 新增可重复构建工具和离线回归：解析动作引用后生成独立资源包，保留 Data/BetaData 隔离、PNG Alpha 与哈希去重，拒绝覆盖已有目录；7 个无可用动作或占位资源跳过并记录清单。资源包不含个人配置、截图模板或日志。
- 本次一并发布下列 0.27–0.29 系列尚未推送的窗口选择、怪物库导入、龙咆哮、Debug 显示和混合后台 DPI 修复。

## 0.29.6 — 2026-09-16

- 修复混合后台/窗口消息截图与 DPI 缩放不一致：在目标窗口 DPI 上下文读取完整原生客户区，再按现有校准客户区尺寸等比例转换。实测修复 1920×1080 内容落在 2880×1620 缓冲区左上角、右侧和底部黑屏造成的识别错位。
- DPI 上下文仅在截图辅助线程内临时切换并恢复；窗口尺寸中途改变、比例不一致或返回格式异常时丢弃截图并停止本次操作，不按黑边裁切、不回退桌面截图、不改用户配置、识别阈值或输入模式。

## 0.29.5 — 2026-09-16

- 怪物识别默认同时匹配原素材与水平镜像，修复素材只有单一朝向时另一侧怪物漏检；图像与 Alpha 同步镜像并缓存，不改磁盘文件和素材计数。
- 左右候选共用现有阈值、场景特征和统一去重，保留分类与过滤语义；姓名牌、头部、称号和过滤项不启用镜像。用户提供的怪猫截图离线复测由 2 只提升为 5 只，不代表严重遮挡或后台截图问题已解决。

## 0.29.4 — 2026-09-16

- Debug 战斗区左下角显示本帧怪物数与策略范围内有效数量，过滤项和旧保留框不计入；零只明确显示 0，随怪物 Debug 和 F7 总开关显隐，采集预览同步保留数量快照。

## 0.29.3 — 2026-09-16

- 区域校准页新增“截图时保留 Debug 框”开关，默认关闭并持久保存。开启后冻结预览叠加当前识别框，小地图放大时同步映射；原始采集素材保持无框，取消/异常后清理预览状态，不改变 F7 设置。

## 0.29.2 — 2026-09-16

- 龙咆哮直接沿用公共平台安全点，不再要求单独采集定点；移除专属定点按钮，旧专属点保留但不参与决策。已有效采集的平台安全点直接生效，未采集或已失效时仍阻止启动和回位。

## 0.29.1 — 2026-09-16

- 血条、蓝条不再作为挂机基础校准条件；仅开启全局自动喝药时要求采集。小地图、玩家标记、战斗区和策略专属依赖仍需完成，面板标注血蓝条为可选，不清除已有采集。

## 0.29.0 — 2026-09-15

- 龙咆哮改为独立小地图定位，不再要求或执行姓名牌、头部识别；仅唯一实时玩家标记有效时允许回位和定点施法。
- 使用固定战斗识别区统计本帧怪物，旧角色相对攻击区域保留但不再使用；暂停喝药时仍显示怪物识别。严格超过数量阈值才施法，偏离先回位，标记丢失/歧义停止战斗移动，保留窗口、补给、冷却和移动超时保护。其他策略的视觉定位门禁不变。

## 0.28.1 — 2026-09-15

- 本地资源库导入素材默认在识别加载时按 1.5 倍最近邻放大，Alpha 同步缩放；已导入素材自动适用，不改原 PNG、不累乘放大，截图采集素材保持原尺寸。

## 0.28.0 — 2026-09-15

- 新增本地怪物资源库入口：选择 NewMaple 解包目录，按中文名字或 ID 搜索、预览动作帧，添加到当前怪物分类；无需连接游戏窗口。
- 正式服与测试服独立选择；只列出有对应资源的怪物，支持 UOL/Canvas/整体怪物引用，循环、越界和跨数据集引用拒绝导入。
- 导入保留 Alpha、裁掉透明空边、跳过重复帧，不覆盖已有素材；单次最多 60 帧，导入后沿用现有模板管理及回收站。经典服档案禁止混入此 NewMaple 资源。

## 0.27.0 — 2026-09-15

- 窗口列表只显示名字，同名窗口追加编号；隐藏句柄、进程号与程序路径。新增“定位窗口”闪烁对应标题栏和任务栏，方便双开辨认，不切前台、不发键；相同候选集在两个面板的编号一致。
- 连接沿用 HWND/PID 会话校验与安全切换，不按标题重新查找；刷新保留具体窗口身份，失效后不自动选择同名替代窗口。句柄不保存为下次启动默认值。

## 0.26.0 — 2026-09-15

### 新增弓箭手「箭雨」策略

- 基于原地攻击新增独立策略与参数档案，普通攻击使用通用攻击键，近身技能可独立设置；包括拾取途中清怪在内，所有攻击均不调整面向。原原地攻击、动态弓箭手及其他策略保持原行为，不自动切换用户当前策略。
- 箭雨左右射程默认各 300 px，可直接输入调整（10–2000 px）。先按通用索敌区筛选，再优先攻击射程内目标；只有射程外目标时，向本帧目标左右靠近，进入范围后停走施法。移动继续检查小地图进展，不盲追旧目标、不自动跨层或绕障。
- 偏离安全点时先处理有效目标，连续 0.4 秒无目标再普通回位；保留原路线拾取、近身技能及安全返程保护。视觉无目标不代表已验证怪物死亡，持续刷怪可能推迟普通回位；关闭拾取、点位失效或定位异常等安全处理不被清怪覆盖。
- 新增「定时向右小步」独立开关，默认开启，修改即保存并热更新；关闭只阻止新周期小步，不影响追怪、拾取及回位。已开始的小步仍完成有限时抬键和验证收尾，重新开启沿用原计时。

### 索敌显示与连续施法优化

- 修复箭雨下调整通用索敌范围时 HUD 有效索敌区不一致的问题。黄色「有效索敌区」与前/后/上/下参数及候选筛选同步，粉色「箭雨施法范围」单独显示左右像素射程，标签错开避免遮挡；两者共用索敌区 Debug 显隐，玩家屏幕定位缺失时不显示旧框。
- 箭雨显式按实际按下技能键的起点计算连续施法间隔，不再额外叠加按键保持时间。其他策略保留原默认计时；公共 last_attack 仍记录成功抬键完成，发送失败不消耗冷却，不新增补发线程或主动切窗。
- 已在原地连续施法时，怪物偶发漏检最多续攻 0.25 秒：必须有本帧真实玩家视觉、唯一实时标记、固定位置及尺寸证据，目标仍在当前搜索与施法范围内。旧证据不滚动续期，移动、补给/定位中断、暂停、明确过滤或超范围即断链；首次等待施法不能使用旧目标，旧目标永不触发追怪。
- 攻击日志新增间隔基准、实际发键起点间隔和按键保持时长；箭雨行动日志每秒汇总状态/原因帧数，并记录原始、过滤后及保留候选数量、距离和视觉新鲜度，帮助区分漏检、射程外、追怪、回位与补给停攻。诊断写盘失败不干扰行动。

### 可选技能键清空

- 可留空的策略技能键新增「清空」按钮，解决原地攻击近身键采集后无法置空的问题；清空后保存当前档案并重载，无需连接或激活游戏。沿用按键修改时暂停挂机的安全流程，必须手动重新启动；必需的通用攻击/移动键不受影响，Esc 仍仅取消采集。

### 验证与数据保护

- 新增策略隔离、范围边界、无方向施法、追怪/拾取回位、周期开关、短暂续攻、计时与面板清空等回归；全量 881 项测试通过。
- 使用隔离输入模拟核验四种输入模式的「箭雨 → Buff 准备 → 施放 → 保护 → 恢复箭雨」流程。Buff 仍优先于策略攻击，保留 0.45 秒准备、0.18 秒按键保持及 1.2 秒保护；日志仅证明按键发送，不宣称游戏已实际获得 Buff 或达到指定攻击频率。
- 更新 README、策略接口规范及 classic/NewMaple 示例配置；不修改或提交个人 config.json、备份、采集素材、素材分类占位目录及运行日志。更新后完全退出助手再启动生效。

## 0.25.0 — 2026-09-13

### 玩家定位与头部接替

- 修复黑白姓名板模板颜色差值全零导致归一化匹配失效、分数始终为 -1 的问题。仅姓名板显式启用无色差模板的灰度与轮廓匹配，彩色模板、怪物等其他匹配通道保持原有规则；字形身份校验、空间约束及正常阈值不降低。
- 头部半分辨率检测没有候选时，在已确认本人身份、存在旧视觉锚点的条件下，仅旧位置附近追加一次原分辨率复核，仍使用原头部门槛。
- 本人头部恢复增加独立有限确认窗口：0.9 秒内至少 3 次真实头部命中，最多容忍 1 个漏帧；要求唯一小地图标记、相对固定基准位移不超过 0.5 像素、固定背景稳定、头部位置偏差不超过 4–12 像素。移动、候选冲突、尺寸变化、超时、暂停及真实视觉重新记录均清空窗口；漏帧本身不返回旧框或恢复攻击。
- 姓名板与首次辅助身份仍采用原严格连续确认；称号不能参与头部漏帧累计。本次不修改攻击、移动、拾取及各输入模式的投递逻辑。

### 定位失败诊断留样

- 默认新增只读定位诊断，可用 diagnostics.player_localization=false 并重启关闭。复用现有匹配结果，记录搜索区域、倍率、门槛、最高分位置、候选身份校验、首个失败状态、最近 32 帧简要状态、确认链、小地图证据与执行后的状态。
- 挂机期间真实视觉连续缺失至少 2 秒，异步保存最近正常帧、丢失约 2/7/12 秒和恢复帧的战斗区/小地图 PNG 与对应 JSON；更久仍每 5 秒记录文字。暂停单独结案，不计入挂机故障时长。
- 使用有界队列，每会话最多 60 组图片、诊断目录总量上限 256 MiB；达到上限不自动删除旧证据，诊断故障不改变挂机状态。仅保存客户区裁剪图，不截整个桌面、不上传，运行输出不提交仓库。
- 修复新增 localization_diagnostics 耗时阶段未注册导致视觉线程退出的问题；补充真实会话循环的阶段名、汇总显示及窗口会话结束时诊断线程清理回归。

### 更明显的人工验证提示音

- 将短系统提示音替换为高低双音交替提示，重复约 10 秒自动结束；试听按钮使用同一段声音，重复播放不会叠加或无限循环。
- WAV 首次生成后缓存，异步播放，不修改系统音量、不抢焦点、不暂停挂机；原连续命中确认、旧消息锁存和 60 秒提醒限频不变。不识题、不作答、不发送验证命令。

### 集成与验证边界

- 基于远端 0.24.3 合并，保留窗口选择、龙咆哮策略、姓名字形缓存及新版界面；适配新的会话清理生命周期，补齐龙咆哮模拟帧的诊断字段，修正布局测试对字体/DPI 的固定宽度假设。
- 增加灰度模板、头部漏帧窗口、定位留样、异步提示音和性能阶段回归；合并后全量 841 项测试通过。已有 25 张失败留样中，19 张经姓名板修复通过原身份校验；另 6 张仍未恢复，静态回放不等于连续实战验证，不宣称已解决所有遮挡。
- 更新后需完全退出助手再启动生效；不修改或提交个人配置、采集素材、素材分类占位目录与运行日志。

## 0.24.3 — 2026-09-13

- 修复窗口会话目标被怪物目标覆盖的问题，避免连续识别的下一帧崩溃或漏掉窗口身份复核；窗口选择与旧命令行模式保持各自的目标解析路径。
- 新增多帧有怪/无怪及旧命令行回归，发布包含窗口选择、龙咆哮定点策略、分页面板与花蘑菇图标。

## 0.24.2 — 2026-09-13

- 工具窗口与后续子窗口使用 BeiDou 素材库中的花蘑菇图标，直接复用透明 PNG，不修改原素材；Windows 设置独立应用标识，避免使用通用 Python 应用分组。
- 图标放在固定的 `resources/`，与游戏档案、个人采集素材隔离；缺失或加载失败不影响启动。不更改批处理文件关联、系统图标或游戏输入逻辑。

## 0.24.1 — 2026-09-13

- 整体界面调整为 macOS 风格：中性灰白背景、石墨色文字、系统蓝主操作，统一分段式页签、圆角卡片和按钮；成功与停止状态分别使用绿色和红色。
- 窗口连接、功能分组、性能与模板预览统一外观，去掉模板区重复的嵌套方框；保留七个横向页签、窄窗口短标题和固定外层狩猎验证开关。
- 保留 Windows 原生按钮交互与中文字体，样式更新不修改参数、窗口会话或挂机/补给逻辑，不安装 Apple 字体或修改窗口过程。
- 增加主题、圆角控件的状态/尺寸与紧凑布局回归，防止高缩放下控件裁切及重复重绘。

## 0.24.0 — 2026-09-13

- 新增“战士·龙骑士 → 龙咆哮·定点”：独立采集小地图定点、增删/重框攻击范围；范围跟随角色但不随面向翻转，多个范围取并集，重叠不重复计数。
- 只统计当前帧已过滤、去重的怪物，数量严格超过配置阈值才原地施放技能（默认超过 2，即至少 3 只）。独立技能键留空不施放，独立间隔限频，不转向、不追怪、不定时走动、不自动拾取。
- 偏离定点后优先左右走回或上下跳回；回位采用较小到达容差避免边界抖动，保留实际位移验证及超时停止。完全丢失定位时不盲走，仅有可信小地图时只回位或等待，不攻击；不提供绕障碍、爬绳或跨地图寻路。
- 作用窗口选择移至独立“窗口连接”页签；狩猎验证开关放到固定外层，切页可操作且不影响挂机状态；缩减固定区域和重复说明的纵向占用。
- 新增策略边界、采集失效/档案隔离、施法执行安全与紧凑面板回归；原有职业策略和个人配置保留。

## 0.23.0 — 2026-09-13

- 控制面板改为清新的浅灰白底与薄荷绿强调色，统一常规字号、按钮、输入框和下拉列表，减少粗体、深色嵌套边框及重复装饰。
- 功能拆分为横向六页签：区域校准、模板采集、职业策略、按键补给、运行设置、性能监控；各页独立滚动和保留位置，不再把全部功能堆在一条长页面中。
- 顶部固定作用窗口选择，底部固定启动/暂停、自动喝药、识别框与保存操作；切换页签不启动输入、不切换游戏、不丢失未显示页的设置。
- 顶栏只显示简短连接状态，血蓝与通知另行显示，长说明按可用宽度换行；滚轮仅影响所在页，不再全局接管模板管理器和其它窗口的滚动。
- 新增浅色控件与页签布局回归，以及仅使用临时示例配置、禁止连接游戏的隔离 UI 预览工具。

## 0.22.0 — 2026-09-13

- 控制面板可先于游戏启动；新增作用窗口列表、刷新、连接与断开，原配置作为默认候选，连接后仍需手动启动挂机或开启自动喝药。
- 运行、模板采集、区域校准和按键采集共用当前明确选择的窗口，不再各自按标题重新寻找；同名多窗口可区分，目标失效不会自动转连其它窗口。
- 切换窗口先停止旧会话、释放按键并关闭自动喝药，再重新建立识别会话；旧人物身份、小地图基准、路线和待执行请求不会带到新窗口。关闭或最小化目标后面板保留，可恢复窗口再连接。
- 沿用当前档案的参数、校准与素材，仅记住所选窗口标题及程序名供下次默认选择，不保存临时窗口句柄或进程号；NewMaple 与 classic 档案继续隔离。
- 新增选窗、会话生命周期、采集目标一致性及输入目标身份保护回归；独立命令行采集和 `--overlay-only` 保留按原配置查找窗口的方式。

## 0.21.1 — 2026-09-13

- 缓存姓名板模板的固定字形和像素统计，减少同一模板在多候选、多帧验证中的重复计算；现场候选仍逐帧重新提取，身份评分、阈值、Alpha 和遮挡保护保持原有规则。
- HUD 内容未变化时复用已有画布，位置未变化时省去重复的 Tk 窗口布局；仍持续维护与游戏窗口的置顶顺序，采集隐藏/恢复、后台显隐、Debug 开关和退出按钮保持可用。
- 修复运行中修改 FPS 后节流仍使用启动时旧值的问题：配置应用后的下一帧同时更新帧预算和性能监控目标，保留最低 2 FPS。
- 相同内容重复自动保存不再重写配置与备份；`config.json.bak` 保留上一次不同的有效配置，减少同步磁盘写入并避免退出保存覆盖有用备份。
- 新增评分等价、缓存重载、HUD 重绘与显隐、配置备份、真实循环帧率更新回归，以及仅使用合成图的姓名板基准脚本。本轮不包含整帧配置一致性重构。

## 0.21.0 — 2026-09-12

### 狩猎验证人工提醒

- NewMaple 新增独立、可持久保存的“狩猎验证：仅检测并响铃（不暂停挂机）”开关，默认关闭，并提供试听提示音按钮。
- 复用游戏截图，在配置的聊天区域匹配“【狩猎验证】”固定字形；每 0.5 秒检测一次，连续 3 次命中后异步播放系统提示音。不是通用 OCR，不读取数学题、不解题、不生成或发送聊天命令，也不改变攻击、喝药、Buff 或挂机状态。
- 同一条提示持续存在或滚动时不反复响铃；连续消失 10 秒才解除锁存，两次提醒至少间隔 60 秒。截图中断不算连续命中或连续消失，classic 档案不使用 NewMaple 字形。
- 记录提醒时间、关键字、匹配分数和仅提醒标记；显示最近提醒与模块错误，并加入性能分段统计。声音/识别异常只影响提醒器，日志写失败不能中断挂机或抑制已检测到的提示音。
- 人工输入聊天前仍需自行暂停并关闭自动喝药。聊天隐藏、字体/颜色/布局变化、截图缓存和系统静音可能影响提醒；旧提示出现不代表验证仍有效。

### 混合后台输入、攻击节奏与转向

- 普通攻击、近身技能、Buff、喝药和拾取按当前焦点路由：游戏在前台时使用 SendInput，在后台时使用 PostMessage；每次按下和抬起保持原通道，不因投递失败回退全局输入。
- 混合后台战斗转向不再主动激活游戏；后台普通攻击和跳攻沿用游戏当前朝向，只发送技能，HUD 明确提示当前朝向未确认。实际周期短步、路线拾取、安全回位等导航仍可切前台并留在游戏窗口，不恢复之前窗口。
- 新增 allow_focus=False 的输入层复核，避免检查与发键之间的焦点竞争导致抢前台；后台完全丢失画面定位时等待，不主动左右移动找人。纯独立后台继续保持不发送全局按键的承诺。
- 取消纯鼠标位移造成的混合移动中断和空闲计时重置；键盘/鼠标按钮、窗口身份、焦点、按键期限、行动心跳和 watchdog 保护仍保留。
- 成功发送自有键 keyup 后增加有界 80 ms 异步状态宽限，仅排除刚释放的自有键及修饰键别名。区分“游戏已在前台等待按键空闲”与“等待切到前台”，不再将自有按键滞后泛化成用户操作。
- 所有模式取消逐次攻击持方向和每 5 秒强制停攻转向；普通药/Buff 不再单独清除朝向缓存，方向/跳跃键绑定除外。短暂视觉缺失保留有界缓存，真实移动、焦点、身份及长期定位失效仍重新转向。
- 纯前台移动/拾取后的恢复攻击增加跨帧交接：释放方向与拾取，稳定等待 120 ms，方向短按至少 80 ms，再等 120 ms；保留较长个人配置及上次技能停攻条件，同侧持续输出不增加重复转向。

### 路线长按拾取与清怪方向

- 去程、到点及正常返程均先原地清理有效怪物，期间停走、停拾取；连续无有效目标 0.4 秒后才继续。关闭开关、点位失效、掉层及定位异常的安全返程仍优先回位。
- 清怪优先清当前侧，当前侧连续缺失 0.4 秒才换边；只使用本帧索敌区域内的有效候选，不保存旧目标框继续攻击。原侧恢复或药/Buff/定位中断重置换侧确认，清怪结束清除侧锁。
- 前台路线清怪中的每次必要转向都采用加强交接，不再只保护移动后第一击；混合及其它输入模式的转向时序保持隔离。返程清怪短暂丢失视觉时先等待原定位宽限，不立即插入回家移动。
- 路线拾取改为持续持有拾取键，仅该长按键约每 0.1 秒补键盘自动重复 keydown，中途不 keyup；普通攻击和方向键不增加重复。
- 修复首次前台长按未启动重复/超时线程的问题，每帧续期、约 0.8 秒无续期独立抬键。遇怪、药/Buff、定位丢失、暂停、结束和分段时释放；重复发键失败先释放再报告。
- 修复混合到点拾取被每帧结束移动租约抬掉，以及申请新租约前自有拾取键阻塞空闲检查的问题；保留输入通道归属、分段、无进展和失焦保护。

### 姓名板复核、测试与已知限制

- 姓名板半分辨率粗检没有有效身份候选时，在同一 ROI、同一帧特征上追加原分辨率复核，改善细字一像素偏移造成的漏检；不降低正常阈值，不绕过身份消歧、位移约束或连续帧确认。
- 增加并更新输入、转向、长按拾取、定位复核和提醒测试；完整回归由 447 项增至 **574 项通过**。同步 README、策略接口约定和详细代码审查记录。
- 2026-09-11 运行日志中的“仅小地图定位、等待画面恢复”长时间漏检已完成诊断，具体图像原因仍待取样排查；本次不宣称已修复，也未因此放宽定位门禁。
- 提醒已通过用户提供的两张静态截图验证；自动化输入序列测试不等于客户端接受动作或实际朝向验证。本次提交没有新增游戏内实测，没有修改或提交个人配置、截图、采集素材、素材分类占位目录及运行日志。

## 0.20.0 — 2026-09-06

### 混合后台移动与异常保护

- 新增显式选择的 `hybrid` 模式：实际移动、回位跳跃、周期短步和找人位移使用独立的前台 SendInput 通道；攻击、转向、近身技能、Buff、喝药及拾取仍走 PostMessage，不改变纯独立后台的输入隔离承诺。
- 移动前等待键鼠空闲 0.7 秒，验证游戏窗口与进程身份，激活后等待新视觉帧；窗口激活放在限时子进程，输入具备会话、按键续期、行动心跳及独立 watchdog 保护。路线分段保留移动无进展监控。
- 修复移动结束恢复旧窗口失败导致视觉线程退出的问题。按用户授权改为“混合后台（移动后留前台）”：结束、中断、暂停只抬键，不再自动恢复原窗口；用户自行切走时不抢回。
- 混合输入异常在公共行动边界暂停并提示，不再穿透到 worker；故障期间不继续暂停喝药，须显式重新启动并绑定，移动键未释放时禁止清除错误继续运行。

### 原地定时路线拾取

- 原地策略新增可开关的同平台路线拾取，目标点像安全点一样在冻结放大的小地图中点选；默认间隔 180 秒，采样与安全点保持独立，重采小地图会使旧点位失效。
- 去程沿路拾取，遇有效怪物先停下清怪（支持近身技能）；到点拾取结束后返回安全点，返程优先回位、不攻击，回到安全点再恢复输出。关闭功能时结束拾取并回位，暂停立即释放按键。
- 区分“到点前最长秒”（默认 90 秒）和“到点有效拾取秒”（默认 1.5 秒）；有效拾取单独累计，清怪、药/Buff、定位中断只暂停累计、不清零，未实际发送拾取键不计时。
- 增加默认 1 秒的短暂定位丢失宽限，先停下等待恢复，超过才转返程；完全无可信位置仍停止。到点阶段另有默认 60 秒总上限，防止无限清怪；相关秒数可直接输入，并保留用户已有数值。
- 记录明确返程原因及限频进度；确认实际离开并回到安全点后，重新计算拾取间隔及 45 秒周期右移，完成一次拾取往返可替代本轮右移。

### 原地近身技能与攻击朝向

- 原地策略新增独立近身技能采集，参考弓箭手动态共用距离计算；按怪物检测框宽度归一化，默认进入/退出阈值为 0.35/0.9 倍，支持调整，清空按键即关闭，远离后恢复通用攻击键。
- 修复原地攻击只相信缓存朝向的问题：首次输出、换边和移动/药/Buff/定位中断后，距上次攻击至少 0.6 秒再独立短按方向至少 0.06 秒，等待 0.08 秒并在后续帧复核目标后攻击。
- 同侧持续输出每 5 秒重发转向，避免一次方向键被技能动作吞掉后长期打反；不在每次攻击时持续按方向，保留原输入模式与自定义较长转向按键时长。
- 转向准备期间显示转向状态，攻击日志明确 `facing_verified=false`：HUD 和日志的攻击方向是输入意图，不是角色实际朝向的视觉确认。

### 小地图遮挡补位与主导航

- 在既有姓名板身份与视觉/唯一小地图标记配对基础上，新增固定背景纹理校验；静止标记位移不超过 0.5 像素且背景稳定时，允许默认 3 秒（最多 5 秒）的遮挡补位。
- 保留旧 0.2 秒补位规则；延长补位不刷新身份、视觉时间、速度或固定基准。移动、多候选、背景变化、证据不足或超时立即失效，不允许移动后回原点复活旧定位。
- 仅剩可信小地图位置时，支持默认 10 秒（最多 30 秒）的主导航回安全点/安全区；清空屏幕目标，只允许安全导航和等待，禁止攻击、追怪、拾取及新周期短步。到期停止，不盲目找人。
- 公共策略接口增加地图导航限制、会话运行状态和中断信息；路线状态归运行会话所有，不放入全局策略实例。面板新增点位采集和实时开关元数据，配置与 NewMaple 示例同步。

### 独立诊断与验证

- 增加显式授权的线程键盘状态共享探针和前台短步探针：默认不发键，真实测试需确认其它发键器已退出，使用隔离子进程、限时释放、窗口身份和用户操作检查，不接入日常策略。
- 保留实测结论及失败记录：线程状态共享未观察到后台位移，前台短步观察到位移但恢复旧窗口失败；不能把 API 成功当作游戏接受移动，也不宣称纯后台移动已解决。
- 更新 README、策略扩展约定及代码审查记录；完整回归测试 **447 项通过**，覆盖混合输入安全、地图定位边界、路线计时/中断、近身技能和转向时序。
- 本次未重新进行游戏内实测；混合模式仍会短暂影响前台，方向短按可能造成微小位移，独立后台仍不支持最小化。个人配置、采样图片、素材分类占位目录和运行日志不提交。

## 0.19.0 — 2026-09-05

### NewMaple 客户端与独立档案

- `Start.bat` 默认启动 `newmaple` 档案，按 `NewMaple.exe` / `NewMaple` 识别桌面客户端；`Start.bat classic` 保留原怀旧服入口，CLI 支持显式选择档案。
- 新增 `profiles/newmaple/config.example.json` 和独立素材目录；校准、五类模板加载/采集/管理/回收、分类统计及面板均按当前档案解析路径，避免复用 classic 的个人数据。
- 改进启动参数、面板档案标识与异常处理；个人配置、配置备份、采集图片和运行日志不纳入本次提交。

### 全局自动喝药与三组定时 Buff

- 自动喝药开关改为真正的会话级全局开关：关闭时，挂机与暂停状态都不发送 HP/MP 药键；开启后，暂停状态仅在游戏位于前台时补药。
- 新增三组 Buff，分别支持按键采集、秒数直接输入及独立持久化开关；关闭单项保留按键、间隔和上次发送时间，空按键或零间隔同样禁用。
- Buff 仅在挂机运行时调度；暂停恢复不重置冷却，同到期每帧最多一个，按最早到期顺序避免短间隔槽位长期抢占。
- 施法采用跨帧 0.45 秒停攻准备、0.18 秒按键保持及 1.2 秒保护；准备期间可取消，发键前复核窗口/退出/暂停及前台要求，独立后台模式允许失焦补 Buff。
- 所有下拉框禁止滚轮改值，降低滚动控制面板时误改职业、策略、分类和输入模式的风险。

### 玩家识别、技能选择与回位

- 姓名板身份已建立后，允许在受限预测位置使用较低阈值恢复；头部/称号补位保留距离、身份、时间与连续帧约束，不放宽首次全图认人规则。
- 连续重获确认按实际视觉帧计数，空帧/歧义打断确认链，同帧重复扫描不增加帧数；增加丢失/重获日志，改善动画、遮挡后的定位续认。
- 小地图颜色相近的校准提示结合实时唯一性判断，避免仍可正常识别时被不必要地阻止启动。
- 弓箭手动态策略新增 AOE、单体和近身技能采集：聚怪数量/距离决定 AOE，贴身使用近身技能；距离按怪物检测框体型归一化，进入/退出阈值分离并可调整。
- 动态攻击换边先准备转向并等待下一帧确认，再发送技能；保留独立转向与方向键异常释放，减少提示朝向和实际攻击朝向不一致。
- 原地攻击平时偏离小地图安全点也优先回位；默认每 45 秒停攻后向右小步，采用跨帧准备、输入层限时抬键和实际位移验证，不再把发键当作移动完成。
- 小步最多三轮尝试，正确方向位移才更新周期计时；普通回位连续 2 秒无进展重试、4 秒无进展暂停，记录移动开始/进展/重试/失败。补药、Buff、暂停与识别中断安全释放移动键。
- 修复标飞安全区 Y 判断混用战斗画面与小地图坐标；保留远程 0.18.0 的近目标跳跃攻击和默认关闭安全区巡逻行为，并同步 NewMaple 示例配置。

### 独立后台实验与稳定性

- 新增 `window_message` 模式：只向绑定的游戏输入窗口发送消息，不回退全局 SendInput，不伪造焦点；旧 `background`/`postmessage`/`window` 兼容扫描码含义保持不变。
- PrintWindow 截图隔离在可终止的辅助进程；启动/截图设置超时，失败、空白或最小化暂停，不回退桌面截图。独立后台不切前台、不置顶，失焦时隐藏 HUD。
- 修复暂停时实际抬键和重复线程竞态；移动消息增加抬按脉冲尝试、独立限时释放，重复投递错误交回主循环处理。
- 配置使用同目录临时文件、flush/fsync 与原子替换，保留上一份可解析的 `.bak`；视觉线程异常由 Tk 主线程消费，避免延迟回调丢失异常变量。
- 新增只读截图诊断与显式授权的独立移动兼容探针，覆盖同步/异步消息、参数、重复状态位和左右方向；默认不发键，实测限时并检查焦点、目标身份和停止条件。

### 验证与已知限制

- 合入远程 0.18.0 后完整回归测试 **293 项通过**，覆盖档案隔离、全局喝药、Buff 调度、识别恢复、技能分流、回位及后台输入/截图安全。
- **NewMaple 独立后台攻击已获实测确认，但后台移动仍未解决**：右移脉冲三轮无位移；剩余 16 组纯窗口兼容对照亦均无位移，即使 API 调用成功也不能认为游戏接受移动。未将失败的试验参数推广到正式输入配置。
- 独立后台不支持最小化；非空缓存画面尚不能可靠判为冻结。整帧配置快照一致性、多实例配置覆盖与进一步性能优化仍待处理，详见 `CODE_REVIEW.md`。

## 0.18.0 — 2026-08-16

- 标飞新增可调的近目标每次跳跃攻击：目标进入设定水平距离后，每次攻击优先执行跳跃攻击，并沿用公共攻击间隔。
- 标飞安全输出区内无目标巡逻改为默认关闭，可在策略设置中独立开启；补充相关配置、面板说明和回归测试。

## 0.17.0 — 2026-08-15

- 标飞新增多个独立索敌区：区域跟随稳定角色锚点移动、保持屏幕方向且不随面向翻转，支持区域优先级、最近距离或识别分选敌，并可选叠加通用索敌框。
- 标飞安全输出区改为在放大的小地图上框选，回位和无目标巡逻统一使用小地图玩家标记，战斗镜头滚动不再导致安全区漂移；旧战斗画面安全区自动失效并要求重采。
- 新增索敌成功后只改变面向的动作，以及安全区内无目标左右巡逻、边缘折返和可调巡逻边距；玩家姓名板超时丢失时可按可调时长交替左右位移恢复。
- Debug 框改为默认全部显示、各采集项独立开关，面板按钮同步显示开关状态；标飞安全区显示在小地图上，多索敌区继续显示在战斗画面。
- 修复日常启动闪退与 Python 环境检测流程，保留 `Start.bat` 为唯一日常入口；扩展配置迁移、采集、策略、输入安全和界面回归测试至 209 项。

## 0.16.0 — 2026-08-15

- 弓箭手动态的小地图平台安全点改为二维回位：水平越界优先左右移动，掉到下层时按间隔向上跳回，位于上层时下跳；新增水平/垂直容差、跳跃间隔和下方向键配置。
- 弓箭手攻击与追踪目标改为按玩家稳定攻击锚点到怪物中心的二维实际距离排序，优先处理最近单位；原地攻击和标飞策略保持原有排序。
- 弓箭手每次攻击都重新计算目标方向，先按住方向键完成转向，在方向键仍按下时发出攻击再释放，减少被怪物击退后朝向错乱；异常时也会安全释放方向键。
- 更新策略说明、面板和示例配置，回归测试扩展至 194 项。

## 0.15.0 — 2026-08-15

- 新增小地图静止确认：可靠姓名板命中，以及姓名板身份建立后的连续头部命中，会固定记录同帧本人标记；三路屏幕定位随后暂时丢失时，标记在 2 像素内保持静止即可在最多 0.2 秒内沿用上次视觉位置。
- 静止确认不会刷新视觉或身份时间，也不会逐帧滚动续命；标记移动、缺失、多重命中、尺寸变化或超时会立即清除位置并停止动作，移动后必须重新取得视觉命中才能建立新基准。
- 控制面板新增可自动保存的静止判断开关，HUD 用橙色虚线玩家框区分推断位置，并补充固定基准、像素边界、头部刷新、身份来源和动作清理回归测试。

## 0.14.0 — 2026-08-15

- 控制面板顶部新增可折叠性能监控卡，每秒显示实际 / 目标 FPS、整帧平均与 P95、超预算比例、进程 CPU 和内存；折叠状态会自动保存，旧配置会补齐默认值。
- 视觉主循环新增低开销分段计时，分别统计截图、预处理、怪物识别、玩家定位、策略选敌、决策输入和 HUD 准备；整帧耗时不包含帧率节流等待，独立喝药模式会明确省略未执行的识别阶段。
- 新增线程安全的有界滚动统计器，覆盖截图失败、采集暂停、恢复、视觉线程异常与正常退出状态，并补充确定性 P95、资源采样、并发读写和旧配置兼容回归测试。

## 0.13.0 — 2026-08-14

- 重构玩家遮挡定位：姓名板改为按 Alpha 有效字形区验证并按身份去重，无效的姓名板原始候选不再阻断头部/称号补位；已确认身份后可持续局部跟踪，启动时也可由高置信辅助模板连续 3 帧建立受限身份。
- 小地图玩家标记采集改为最高 8 倍放大点选并映射回原始冻结帧，通过连通高亮色块与唯一性校验生成 HSV 条件，同时保存初始归一化位置供首帧消歧。
- 将弓箭手的“战斗画面平台中心”迁移为“小地图平台安全点”：支持放大采集、显式坐标系与旧配置失效迁移，弓箭手动态策略按小地图位置优先回到安全范围，且运行 HUD 不显示安全点。
- 重做怪物模板生成：基于选区边缘估计背景、提取最大主体 Alpha、自动紧裁并拒绝无法区分的全帧结果；检测新增可配置的轮廓结构权重，减少棕色地形与空气误框。
- 删除仓库内旧的初始姓名板、头部和称号模板，更新控制面板、CLI、弓箭手策略规范、示例配置与使用文档；回归测试扩展至 156 项。

## 0.12.0 — 2026-08-14

- 玩家定位拆分为“名字字形确认身份”和“头部/称号短时补位”：姓名板候选在原分辨率验证唯一名字，遮挡时不再用其他玩家的头部或公共称号建立新身份，远端重获需连续确认后才恢复动作。
- 新采集的姓名板、头部和称号会在同一冻结帧继续采集角色脚底点，并为每张模板保存独立脚底偏移；旧模板继续兼容原有固定偏移，重新采集后可消除不同裁剪范围造成的定位跳动。
- 修复“原地攻击”沿用当前面向的非对称索敌区、导致左侧有怪时仍持续朝右攻击的问题；该策略现在使用左右两个面向索敌区的并集选择最近目标。

## 0.11.0 — 2026-08-14

- 玩家识别改为预测位置局部搜索：稳定时复用统一缩放网格，只扫描角色附近；连续丢失或周期复核时恢复全图姓名板、头部、称号三路融合，并支持传送、换层后的远端安全重获。
- 自动喝药拆为独立控制器，面板新增默认关闭的会话开关；暂停挂机时可单独监测血蓝，非挂机状态仅在游戏位于前台时发药键，并跳过战斗模板匹配以降低 CPU 占用。
- 加强补药模式切换、采集暂停、窗口/截图失效和异常按键释放的输入安全；测试扩展到 129 项，并补齐多缩放 ROI、预测候选、旧锚隔离、三路投票及补药安全门回归。

## 0.10.0 — 2026-08-14

- 控制面板删除游戏窗口解耦后已失去作用的右侧说明与实时摘要，收窄为单栏工具；采集、校验、模板管理、职业策略、参数、Debug、保存和启停功能全部保留。
- “挂机时强制游戏窗口置顶”改为可自动保存的选项：默认保持开启；关闭且使用后台按键时，启动挂机不再抢占前台或置顶游戏窗口。
- 更新使用手册中的独立窗口、标飞策略、后台按键和可选置顶说明；测试扩展到 107 项，覆盖关闭置顶后的后台启动路径与单栏控制面板。

## 0.9.0 — 2026-08-14

- 修正采集项“显示”的 Debug 逻辑：行内按钮现在只显示当前采集项，不再误开全部识别框；F7 和底部 Debug 开关仍负责全部显示或全部隐藏。
- 为血条、蓝条、小地图、玩家标记、战斗区、平台中心、通用索敌范围和玩家姓名板建立单项 Debug 过滤，并补上平台中心的十字标记。
- 玩家姓名板从“战斗识别”移入“模板采集”；模板采集和图片管理器均按“怪物模板 / 人物模板”两个子类分组，原有采集、预览、删除、分类和数量统计保持不变。

## 0.8.0 — 2026-08-14

- 工具窗口与游戏窗口完全解耦：移除真实游戏窗口停靠、移动、缩放和退出恢复逻辑，控制台改为独立可移动工具窗口。
- 采集框选层和 Debug 识别框仍直接显示在独立游戏窗口上；挂机时的游戏置顶、暂停恢复、后台按键、采集、策略和实时状态功能保持不变。
- 控制台恢复为适合与游戏并排摆放的常规尺寸，右侧明确显示独立窗口模式、采集方式和实时识别状态。

## 0.7.1 — 2026-08-14

- 修复独立采集时游戏窗口在冻结帧上层打开后又被控制台停靠线程改尺寸，导致下一项采集将上一项误判为失效的问题；现在采集前先稳定停靠尺寸，采集期间禁止重新停靠。
- 血条、蓝条等按归一化坐标保存的项目在等比缩放后继续有效；仅当游戏客户区宽高比变化超过 3% 时，才要求重新校准。

## 0.7.0 — 2026-08-14

- 控制面板重构为采集控制台：左侧保留全部采集、怪物分类、模板管理、职业策略和参数入口，中间停靠真实游戏窗口，下方显示实时识别状态，底部集中保存、Debug、启动/暂停和退出操作。
- 游戏窗口采用独立 HWND 停靠而非 `SetParent` 嵌入；仅在助手权限足够时移动，退出后恢复原始窗口状态，挂机期间的强制置顶与暂停恢复规则保持不变。
- 血条、蓝条、小地图、小地图玩家标记、战斗识别区和平台中心改为单项采集、单项保存；重采小地图或战斗区时只使对应依赖项失效，旧组合采集入口继续兼容。
- 职业与基础策略分层选择，策略布尔模块使用多选；标飞安全输出区保持可选，新增可关闭的近身重叠跳跃攻击、默认 20% 水平重叠阈值和 Alt→Shift 跳攻时序。
- 新增窗口尺寸校验：停靠后尺寸与上次校准不一致时阻止启动并提示逐项重采，避免旧坐标在新客户区中被误判为有效。

## 0.6.0 — 2026-08-14

- 新增“标飞安全输出”职业策略及专属二维安全输出区框选：玩家低于安全区时优先按住水平回位方向并周期性向上跳，进入安全区后才允许输出。
- 标飞只选择角色下方且位于通用索敌区内的怪物，同层目标会被过滤；默认不追怪、不巡逻、不自动拾取，玩家位于安全区上方的下跳回位暂不处理。
- 新增可配置跳跃键、回位跳跃间隔、目标最小向下高度差，以及 F7 Debug 框中的标飞安全区显示；旧配置会安全补齐默认值但保持“尚未框选”状态。

## 0.5.1 — 2026-08-14

- 修复首次启动缺少个人 `config.json` 时，隐藏控制台直接退出的问题；现在会自动从示例配置创建一次，且绝不覆盖已有个人配置。

## 0.5.0 — 2026-08-14

- 启动挂机后自动将游戏窗口设为置顶，避免普通窗口覆盖视觉识别区；暂停、安全停机或退出时自动取消置顶，并在置顶失败时阻止启动且显示原因。

## 0.4.0 — 2026-08-14

- 新增统一的采集图片管理器：怪物模板、过滤项、姓名板、头部和称号勋章均可按类型查看数量、预览原图、继续新增采集，并单张移入项目内的模板回收目录。
- 删除任意类型图片后立即重载运行时识别模板、刷新统计并选中相邻图片；删除最后一张姓名板前会明确提示必须重新采集才能启动挂机。
- 五类模板共用带路径边界检查的列表与可恢复删除接口；玩家三类文件名改用微秒时间戳，避免同一秒连续采集覆盖旧图。

## 0.3.0 — 2026-08-13

- 将战斗识别区与平台中心采集从“状态栏与小地图校准”中独立出来；两步采集复用同一冻结帧，平台中心按战斗区归一化保存，并只作为策略数据、不在 HUD 中显示。
- 新增可扩展职业策略注册体系、策略下拉框、实时逻辑说明和 `STRATEGIES.md` 协作规范；策略配置统一存放在 `strategy.options.<strategy_key>`，公共安全、补药和按键执行继续由运行层负责。
- 默认“弓箭手动态”策略增加平台中心安全半径：角色离平台中心过远时优先回位，再执行区内攻击、同层追踪及可选拾取/巡逻。
- 新增“原地攻击”通用策略：只在公共索敌区内选择左右目标并攻击，不追怪、不巡逻、不回平台中心；同侧连续攻击不重复点方向键，仅换边时转向以减少位置漂移。
- 将弓箭手专属攻击框迁移为所有策略共享的 `targeting.box`，兼容旧配置一次性迁移；默认索敌框总高度提高 20%、总宽度缩短 10%，面板支持直接微调、框选、运行时预览和重启持久化。
- 统一姓名板、头部和称号三种玩家定位来源的脚底攻击锚点，并增加轻量 EMA 平滑与大幅移动快速跟随，解决索敌框随检测来源上下跳动的问题。
- 挂机配置改为自动持久化：策略切换及参数、通用索敌区、数值微调、HP/MP 滑杆和复选框均会在运行时保存；退出前再次完整落盘，最长运行时间默认 `0`（不限时）。
- 更新控制面板、CLI、安装提示、README 和兼容导出；测试扩展到 77 项，覆盖校准拆分、配置迁移与持久化、稳定锚点、策略注册/选敌/动作优先级及原地转向行为。

## 0.2.0 — 2026-08-13

- 怪物模板支持分类、新建、重命名、可恢复删除和图片预览；分类目录带可跟踪占位文件，可随 Git 上传，删除单张模板后管理窗口保持打开并自动选择下一张。
- 新增按分类保存的怪物过滤项，只抑制同分类、同位置的误识别候选；当前只加载面板选中的怪物分类，切换后立即生效并持久化。
- 日常启动入口统一为 `Start.bat`；启动时完成 UAC 并进入输入待命，但仍需手动启动挂机，旧启动与采集批处理入口移除。
- 面板新增与 F7 双向同步的 Debug 框开关；HP、MP 自动喝药阈值改为 0～100% 滑杆及 1% 微调按钮。
- 修复采集提示被运行 HUD 覆盖、全局热键缺少按键映射导入，以及保存配置或打开采集工具时可能遗留一次输入的竞态。
- 怪物分类、过滤项和模板回收目录改为递归忽略；移除仓库中误跟踪的个人怪物模板图片。
- 测试扩展到 60 项，覆盖分类存储、过滤抑制、图片预览、单入口、Debug 开关、阈值保存及输入安全。

## 0.1.3 — 2026-08-13

- 文档：写明 Cursor 与本机 Codex 共用 `AGENTS.md`，并补上本仓库 git 身份与并行开发约定。
- 性能：姓名板稳定定位时将头部、称号检测降为每 0.5 秒复核；姓名板失败、定位过期或模板重载后仍立即恢复三路检测。
- 稳定性：玩家定位保持时间到期后清除旧位置先验，允许传送或长时间遮挡后重新定位。
- 测试：改用可提交的示例配置并移除被同名方法覆盖的重复用例，恢复干净工作区下的完整测试。

## 0.1.2 — 2026-08-13

- 性能：同一帧的怪物、姓名板、头部、称号检测共用一份场景特征（缩放 / 颜色对立通道 / 边缘只算一次）；模板侧特征按缩放比例缓存，识别结果不变。
- 架构：控制面板改为直接从 `mbv` 各模块导入，不再依赖根目录兼容入口。

## 0.1.1 — 2026-08-13

- 去掉已废弃的 OpenCV 校准窗口、预览常量，以及未再使用的玩家检测选择函数。
- 攻击目标筛选共用同一套距离排序；兼容入口只再导出测试和面板真正用到的符号。
- `Start-Observer.bat` 改为调用 `Start.bat`，避免两份脚本各自改丢。

## 0.1.0 — 2026-08-13

- 代码拆到 `mbv/` 包，根目录只保留启动入口。
- 控制面板统一采集、观察与挂机配置。
- 后台按键始终发送扫描码；攻击范围按角色中心和面向框选。
- 模板采集改为冻结当前画面再框选。
- F7 显隐校对 HUD。
